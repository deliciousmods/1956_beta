
add_namespace = syria_french_capitulation_side

country_event = {
	id = syria_french_capitulation_side.1
	title = syria_french_capitulation_side.1.t
	desc = syria_french_capitulation_side.1.d
	picture = GFX_report_event_soldiers_in_france

	fire_only_once = yes
	
	is_triggered_only = yes

	option = {
		name = syria_french_capitulation_side.1.a
		ai_chance = {
			base = 25
			modifier = {
				factor = 50
				has_war = yes
				any_enemy_country = {
					strength_ratio = { 
						tag = ROOT 
						ratio < 0.4
					}
				}
			}
			modifier = {
				factor = 75
				16 = { NOT = { is_controlled_by = FRA } } #Paris	
			}
		}
		every_enemy_country = {
			white_peace = SYR
		}
		if = {
			limit = {
				EFR = {
					exists = yes
				}
			}
			EFR = {
				set_autonomy = {
					target = SYR
					autonomy_state = autonomy_colony
				}
			}
		}
		else = {
			every_other_country = {
				limit = {
					has_country_flag = is_vichy_france
				}
				set_autonomy = {
					target = SYR
					autonomy_state = autonomy_colony
				}
			}
		}
		hidden_effect = {
			set_politics = {
				ruling_party = neutrality
				elections_allowed = no
			}
			#Usual pupet ideology popularites
			set_popularities = {
				democratic = 13.33
				fascism = 13.33
				neutrality = 60.01
				communism = 13.33
			}
		}
		
		promote_character = SYR_henri_dentz
		set_country_flag = SYR_support_vichy
	}
	
	option = {
		name = syria_french_capitulation_side.1.b
		ai_chance = {
			base = 25
		}
		#promote_character = 
	}			
}