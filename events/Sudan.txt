
add_namespace = r56_sudan

country_event = { # sudan sues for peace, should we accept
	id = r56_sudan.1
	title = r56_sudan.1.t
	desc = r56_sudan.1.desc
	picture = GFX_report_event_royal_visit
	is_triggered_only = yes

	option = {
		name = wuw_GER_mutual_guarantees.1.a	#Of course, we must agree.
		ai_chance = { base = 1 }
		every_country = {
			limit = {
				OR = {
					tag = ROOT
					is_ally_with = ROOT
				}
				has_war_with = FROM
			}
			white_peace = FROM
		}
		FROM = {
			country_event = {
				id = siam_r56.28
				hours = 1	#White peace needs to come before transfer to avoid oddities
			}
		}
		custom_effect_tooltip = generic_skip_one_line_tt
		548 = {
			if = {
				limit = {
					owns_or_subject_of = yes
					OR = {
						is_controlled_by = FROM
						controller = { is_subject_of = FROM }
					}
				}
				transfer_state_to = FROM
			}
		}
		904 = {	#Kedah
			if = {
				limit = {
					owns_or_subject_of = yes
					OR = {
						is_controlled_by = FROM
						controller = { is_subject_of = FROM }
					}
				}
				transfer_state_to = FROM
			}
		}
		547 = {
			if = {
				limit = {
					owns_or_subject_of = yes
					OR = {
						is_controlled_by = FROM
						controller = { is_subject_of = FROM }
					}
				}
				transfer_state_to = FROM
			}
		}
		546 = {
			if = {
				limit = {
					owns_or_subject_of = yes
					OR = {
						is_controlled_by = FROM
						controller = { is_subject_of = FROM }
					}
				}
				transfer_state_to = FROM
			}
		}
		905 = {
			if = {
				limit = {
					owns_or_subject_of = yes
					OR = {
						is_controlled_by = FROM
						controller = { is_subject_of = FROM }
					}
				}
				transfer_state_to = FROM
			}
		}
		1055 = {
			if = {
				limit = {
					owns_or_subject_of = yes
					OR = {
						is_controlled_by = FROM
						controller = { is_subject_of = FROM }
					}
				}
				transfer_state_to = FROM
			}
		}
	}
	option = {
		name = wuw_GER_diplomacy.117.b	#We will never submit!
		ai_chance = { base = 0 }
	}
}

country_event = { # we r at peace!!!!
	id = r56_sudan.2
	title = r56_sudan.2.t
	desc = r56_sudan.2.desc
	picture = GFX_report_event_african_soldiers
	is_triggered_only = yes
	immediate = {
		hidden_effect = {
			548 = {
				if = {
					limit = {
						owns_or_subject_of = yes
						OR = {
							is_controlled_by = FROM
							controller = { is_subject_of = FROM }
						}
					}
					transfer_state_to = FROM
				}
			}
			904 = {	#Kedah
				if = {
					limit = {
						owns_or_subject_of = yes
						OR = {
							is_controlled_by = FROM
							controller = { is_subject_of = FROM }
						}
					}
					transfer_state_to = FROM
				}
			}
			547 = {
				if = {
					limit = {
						owns_or_subject_of = yes
						OR = {
							is_controlled_by = FROM
							controller = { is_subject_of = FROM }
						}
					}
					transfer_state_to = FROM
				}
			}
			546 = {
				if = {
					limit = {
						owns_or_subject_of = yes
						OR = {
							is_controlled_by = FROM
							controller = { is_subject_of = FROM }
						}
					}
					transfer_state_to = FROM
				}
			}
			905 = {
				if = {
					limit = {
						owns_or_subject_of = yes
						OR = {
							is_controlled_by = FROM
							controller = { is_subject_of = FROM }
						}
					}
					transfer_state_to = FROM
				}
			}
			1055 = {
				if = {
					limit = {
						owns_or_subject_of = yes
						OR = {
							is_controlled_by = FROM
							controller = { is_subject_of = FROM }
						}
					}
					transfer_state_to = FROM
				}
			}
		}
	}
	option = {
		name = portugal.69.a	#A grand triumph for our nation!
	}
}