LUX_chamberofdeputies_calculate_outcome = {
	
}
LUX_chamberofdeputies_setup_elections = {
	set_variable = { LUX.PD_support = 49.6 }
	set_variable = { LUX.PRL_support = 19.2 }
	if = {
		limit = { has_country_flag = LUX_maulkuerfgesetz_passed }
		set_variable = { LUX.LSAP_support = 31.2 }
	}
	else = { set_variable = { LUX.LSAP_support = 25.7 } }
	if = {
		limit = { has_country_flag = LUX_maulkuerfgesetz_passed }
		set_variable = { LUX.KPL_support = 0 }
	}
	else = { set_variable = { LUX.KPL_support = 5.5 } }
	if = {
		limit = { has_country_flag = strike_demands_accepted }
		set_variable = { LUX.turnout = 50.0 }
		set_variable = { LUX.confidence = 50.0 }
	}
	else = {
		set_variable = { LUX.turnout = 30.0 }
		set_variable = { LUX.confidence = 30.0 }
	}

	set_variable = { LUX.stab_impact = LUX.turnout }
	subtract_from_variable = { LUX.stab_impact = 50 }
	divide_variable = { LUX.stab_impact = 2 }

	set_variable = { LUX.demo_impact = LUX.confidence }
	subtract_from_variable = { LUX.demo_impact = 50 }
	divide_variable = { LUX.demo_impact = 2 }

	set_variable = { LUX.PD_support_loc = LUX.PD_support }
	set_variable = { LUX.PRL_support_loc = LUX.PRL_support }
	set_variable = { LUX.LSAP_support_loc = LUX.LSAP_support }
	set_variable = { LUX.KPL_support_loc = LUX.KPL_support }

	set_variable = { LUX.stab_impact_loc = LUX.stab_impact }
	set_variable = { LUX.demo_impact_loc = LUX.demo_impact }
}
LUX_chamberofdeputies_add_turnout_5 = {
	if = {
		limit = { check_variable = { LUX.turnout > 95 } }
		set_variable = { LUX.turnout = 100 }
	}
	else = { add_to_variable = { LUX.turnout = 5 } }

	set_variable = { LUX.stab_impact = LUX.turnout }
	subtract_from_variable = { LUX.stab_impact = 50 }
	divide_variable = { LUX.stab_impact = 2 }

	set_variable = { LUX.stab_impact_loc = LUX.stab_impact }
}
LUX_chamberofdeputies_add_turnout_10 = {
	if = {
		limit = { check_variable = { LUX.turnout > 90 } }
		set_variable = { LUX.turnout = 100 }
	}
	else = { add_to_variable = { LUX.turnout = 10 } }

	set_variable = { LUX.stab_impact = LUX.turnout }
	subtract_from_variable = { LUX.stab_impact = 50 }
	divide_variable = { LUX.stab_impact = 2 }

	set_variable = { LUX.stab_impact_loc = LUX.stab_impact }
}
LUX_chamberofdeputies_add_turnout_15 = {
	if = {
		limit = { check_variable = { LUX.turnout > 85 } }
		set_variable = { LUX.turnout = 100 }
	}
	else = { add_to_variable = { LUX.turnout = 15 } }

	set_variable = { LUX.stab_impact = LUX.turnout }
	subtract_from_variable = { LUX.stab_impact = 50 }
	divide_variable = { LUX.stab_impact = 2 }

	set_variable = { LUX.stab_impact_loc = LUX.stab_impact }
}
LUX_chamberofdeputies_remove_turnout_5 = {
	if = {
		limit = { check_variable = { LUX.turnout < 5 } }
		set_variable = { LUX.turnout = 0 }
	}
	else = { subtract_from_variable = { LUX.turnout = 5 } }

	set_variable = { LUX.stab_impact = LUX.turnout }
	subtract_from_variable = { LUX.stab_impact = 50 }
	divide_variable = { LUX.stab_impact = 2 }

	set_variable = { LUX.stab_impact_loc = LUX.stab_impact }
}
LUX_chamberofdeputies_remove_turnout_10 = {
	if = {
		limit = { check_variable = { LUX.turnout < 10 } }
		set_variable = { LUX.turnout = 0 }
	}
	else = { subtract_from_variable = { LUX.turnout = 10 } }

	set_variable = { LUX.stab_impact = LUX.turnout }
	subtract_from_variable = { LUX.stab_impact = 50 }
	divide_variable = { LUX.stab_impact = 2 }

	set_variable = { LUX.stab_impact_loc = LUX.stab_impact }
}
LUX_chamberofdeputies_remove_turnout_15 = {
	if = {
		limit = { check_variable = { LUX.turnout < 15 } }
		set_variable = { LUX.turnout = 0 }
	}
	else = { subtract_from_variable = { LUX.turnout = 15 } }

	set_variable = { LUX.stab_impact = LUX.turnout }
	subtract_from_variable = { LUX.stab_impact = 50 }
	divide_variable = { LUX.stab_impact = 2 }

	set_variable = { LUX.stab_impact_loc = LUX.stab_impact }
}
LUX_chamberofdeputies_add_confidence_5 = {
	if = {
		limit = { check_variable = { LUX.confidence > 95 } }
		set_variable = { LUX.confidence = 100 }
	}
	else = { add_to_variable = { LUX.confidence = 5 } }

	set_variable = { LUX.demo_impact = LUX.confidence }
	subtract_from_variable = { LUX.demo_impact = 50 }
	divide_variable = { LUX.demo_impact = 2 }

	set_variable = { LUX.demo_impact_loc = LUX.demo_impact }
}
LUX_chamberofdeputies_add_confidence_10 = {
	if = {
		limit = { check_variable = { LUX.confidence > 90 } }
		set_variable = { LUX.confidence = 100 }
	}
	else = { add_to_variable = { LUX.confidence = 10 } }

	set_variable = { LUX.demo_impact = LUX.confidence }
	subtract_from_variable = { LUX.demo_impact = 50 }
	divide_variable = { LUX.demo_impact = 2 }

	set_variable = { LUX.demo_impact_loc = LUX.demo_impact }
}
LUX_chamberofdeputies_add_confidence_15 = {
	if = {
		limit = { check_variable = { LUX.confidence > 85 } }
		set_variable = { LUX.confidence = 100 }
	}
	else = { add_to_variable = { LUX.confidence = 15 } }

	set_variable = { LUX.demo_impact = LUX.confidence }
	subtract_from_variable = { LUX.demo_impact = 50 }
	divide_variable = { LUX.demo_impact = 2 }

	set_variable = { LUX.demo_impact_loc = LUX.demo_impact }
}
LUX_chamberofdeputies_remove_confidence_5 = {
	if = {
		limit = { check_variable = { LUX.confidence < 5 } }
		set_variable = { LUX.confidence = 0 }
	}
	else = { subtract_from_variable = { LUX.confidence = 5 } }

	set_variable = { LUX.demo_impact = LUX.confidence }
	subtract_from_variable = { LUX.demo_impact = 50 }
	divide_variable = { LUX.demo_impact = 2 }

	set_variable = { LUX.demo_impact_loc = LUX.demo_impact }
}
LUX_chamberofdeputies_remove_confidence_10 = {
	if = {
		limit = { check_variable = { LUX.confidence < 10 } }
		set_variable = { LUX.confidence = 0 }
	}
	else = { subtract_from_variable = { LUX.confidence = 10 } }

	set_variable = { LUX.demo_impact = LUX.confidence }
	subtract_from_variable = { LUX.demo_impact = 50 }
	divide_variable = { LUX.demo_impact = 2 }

	set_variable = { LUX.demo_impact_loc = LUX.demo_impact }
}
LUX_chamberofdeputies_remove_confidence_15 = {
	if = {
		limit = { check_variable = { LUX.confidence < 15 } }
		set_variable = { LUX.confidence = 0 }
	}
	else = { subtract_from_variable = { LUX.confidence = 15 } }

	set_variable = { LUX.demo_impact = LUX.confidence }
	subtract_from_variable = { LUX.demo_impact = 50 }
	divide_variable = { LUX.demo_impact = 2 }

	set_variable = { LUX.demo_impact_loc = LUX.demo_impact }
}
LUX_chamberofdeputies_add_support_PD_3 = {
	set_temp_variable = { support_to_remove = 0.3 }
	if = {
		limit = { check_variable = { LUX.PD_support < 99.7 } }
		add_to_variable = { LUX.PD_support = support_to_remove }

		if = {
			limit = { check_variable = { LUX.PRL_support > 0.299 } }
			add_to_temp_variable = { support_counter = 1 }					# 0.30 x 1
			set_country_flag = remove_PRL_support							# PRL_support = yes
			if = {
				limit = { check_variable = { LUX.LSAP_support > 0.149 } }
				add_to_temp_variable = { support_counter = 1 }				# 0.15 x 2
				set_country_flag = remove_LSAP_support						# PRL_support = yes; LSAP_support = yes
				if = {
					limit = { check_variable = { LUX.KPL_support > 0.009 } }
					add_to_temp_variable = { support_counter = 1 }			# 0.10 x 3
					set_country_flag = remove_KPL_support					# PRL_support = yes; LSAP_support = yes; KPL_support = yes
				}
			}
			else_if = {														# PRL_support = yes; LSAP_support = no
				limit = { check_variable = { LUX.KPL_support > 0.149 } }
				add_to_temp_variable = { support_counter = 1 }				# 0.15 x 2
				set_country_flag = remove_KPL_support						# PRL_support = yes; LSAP_support = no; KPL_support = yes
			}
		}
		else_if = {															# PRL_support = no
			limit = { check_variable = { LUX.LSAP_support > 0.299 } }
			add_to_temp_variable = { support_counter = 1 }					# 0.30 x 1
			set_country_flag = remove_LSAP_support							# PRL_support = no; LSAP_support = yes
			if = {
				limit = { check_variable = { LUX.KPL_support > 0.149 } }
				add_to_temp_variable = { support_counter = 1 }				# 0.15 x 2
				set_country_flag = remove_KPL_support						# PRL_support = no; LSAP_support = yes; KPL_support = yes
			}
		}
		else = {
			add_to_temp_variable = { support_counter = 1 }					# 0.30 x 1
			set_country_flag = remove_KPL_support							# PRL_support = no; LSAP_support = no; KPL_support = yes
		}

		divide_temp_variable = { support_to_remove = support_counter }
		if = {
			limit = { has_country_flag = remove_PRL_support }
			subtract_from_variable = { LUX.PRL_support = support_to_remove }
			clr_country_flag = remove_PRL_support
		}
		if = {
			limit = { has_country_flag = remove_LSAP_support }
			subtract_from_variable = { LUX.LSAP_support = support_to_remove }
			clr_country_flag = remove_LSAP_support
		}
		if = {
			limit = { has_country_flag = remove_KPL_support }
			subtract_from_variable = { LUX.KPL_support = support_to_remove }
			clr_country_flag = remove_KPL_support
		}
	}
	else = {
		set_variable = { LUX.PD_support = 100 }
		set_variable = { LUX.PRL_support = 0 }
		set_variable = { LUX.LSAP_support = 0 }
		set_variable = { LUX.KPL_support = 0 }
	}

	set_variable = { LUX.PD_support_loc = LUX.PD_support }
	set_variable = { LUX.PRL_support_loc = LUX.PRL_support }
	set_variable = { LUX.LSAP_support_loc = LUX.LSAP_support }
	set_variable = { LUX.KPL_support_loc = LUX.KPL_support }
}
LUX_chamberofdeputies_add_support_PD_15 = {
	set_temp_variable = { support_to_remove = 1.50 }
	if = {
		limit = { check_variable = { LUX.PD_support < 98.5 } }
		add_to_variable = { LUX.PD_support = support_to_remove }

		if = {
			limit = { check_variable = { LUX.PRL_support > 1.499 } }
			add_to_temp_variable = { support_counter = 1 }					# 1.50 x 1
			set_country_flag = remove_PRL_support							# PRL_support = yes
			if = {
				limit = { check_variable = { LUX.LSAP_support > 0.749 } }
				add_to_temp_variable = { support_counter = 1 }				# 0.75 x 2
				set_country_flag = remove_LSAP_support						# PRL_support = yes; LSAP_support = yes
				if = {
					limit = { check_variable = { LUX.KPL_support > 0.499 } }
					add_to_temp_variable = { support_counter = 1 }			# 0.50 x 3
					set_country_flag = remove_KPL_support					# PRL_support = yes; LSAP_support = yes; KPL_support = yes
				}
			}
			else_if = {														# PRL_support = yes; LSAP_support = no
				limit = { check_variable = { LUX.KPL_support > 0.749 } }
				add_to_temp_variable = { support_counter = 1 }				# 0.75 x 2 
				set_country_flag = remove_KPL_support						# PRL_support = yes; LSAP_support = no; KPL_support = yes
			}
		}
		else_if = {															# PRL_support = no
			limit = { check_variable = { LUX.LSAP_support > 1.499 } }
			add_to_temp_variable = { support_counter = 1 }					# 1.50 x 1
			set_country_flag = remove_LSAP_support							# PRL_support = no; LSAP_support = yes
			if = {
				limit = { check_variable = { LUX.KPL_support > 0.749 } }
				add_to_temp_variable = { support_counter = 1 }				# 0.75 x 2
				set_country_flag = remove_KPL_support						# PRL_support = no; LSAP_support = yes; KPL_support = yes
			}
		}
		else = {
			add_to_temp_variable = { support_counter = 1 }					# 1.50 x 1
			set_country_flag = remove_KPL_support							# PRL_support = no; LSAP_support = no; KPL_support = yes
		}
	
		divide_temp_variable = { support_to_remove = support_counter }
		if = {
			limit = { has_country_flag = remove_PRL_support }
			subtract_from_variable = { LUX.PRL_support = support_to_remove }
			clr_country_flag = remove_PRL_support
		}
		if = {
			limit = { has_country_flag = remove_LSAP_support }
			subtract_from_variable = { LUX.LSAP_support = support_to_remove }
			clr_country_flag = remove_LSAP_support
		}
		if = {
			limit = { has_country_flag = remove_KPL_support }
			subtract_from_variable = { LUX.KPL_support = support_to_remove }
			clr_country_flag = remove_KPL_support
		}
	}
	else = {
		set_variable = { LUX.PD_support = 100 }
		set_variable = { LUX.PRL_support = 0 }
		set_variable = { LUX.LSAP_support = 0 }
		set_variable = { LUX.KPL_support = 0 }
	}

	set_variable = { LUX.PD_support_loc = LUX.PD_support }
	set_variable = { LUX.PRL_support_loc = LUX.PRL_support }
	set_variable = { LUX.LSAP_support_loc = LUX.LSAP_support }
	set_variable = { LUX.KPL_support_loc = LUX.KPL_support }
}
LUX_chamberofdeputies_remove_support_PD_15 = {
	set_temp_variable = { support_to_remove = 1.50 }
	if = {
		limit = { check_variable = { LUX.PD_support > support_to_remove } }
		add_to_variable = { LUX.PD_support = support_to_remove }
	
		if = {
			limit = { check_variable = { LUX.PRL_support > 1.499 } }
			add_to_temp_variable = { support_counter = 1 }					# 1.50 x 1
			set_country_flag = remove_PRL_support							# PRL_support = yes
			if = {
				limit = { check_variable = { LUX.LSAP_support > 0.749 } }
				add_to_temp_variable = { support_counter = 1 }				# 0.75 x 2
				set_country_flag = remove_LSAP_support						# PRL_support = yes; LSAP_support = yes
				if = {
					limit = { check_variable = { LUX.KPL_support > 0.499 } }
					add_to_temp_variable = { support_counter = 1 }			# 0.50 x 3
					set_country_flag = remove_KPL_support					# PRL_support = yes; LSAP_support = yes; KPL_support = yes
				}
			}
			else_if = {														# PRL_support = yes; LSAP_support = no
				limit = { check_variable = { LUX.KPL_support > 0.749 } }
				add_to_temp_variable = { support_counter = 1 }				# 0.75 x 2
				set_country_flag = remove_KPL_support						# PRL_support = yes; LSAP_support = no; KPL_support = yes
			}
		}
		else_if = {															# PRL_support = no
			limit = { check_variable = { LUX.LSAP_support > 1.499 } }
			add_to_temp_variable = { support_counter = 1 }					# 1.50 x 1
			set_country_flag = remove_LSAP_support							# PRL_support = no; LSAP_support = yes
			if = {
				limit = { check_variable = { LUX.KPL_support > 0.749 } }
				add_to_temp_variable = { support_counter = 1 }				# 0.75 x 2
				set_country_flag = remove_KPL_support						# PRL_support = no; LSAP_support = yes; KPL_support = yes
			}
		}
		else = {
			add_to_temp_variable = { support_counter = 1 }					# 1.50 x 1
			set_country_flag = remove_KPL_support							# PRL_support = no; LSAP_support = no; KPL_support = yes
		}
	
		divide_temp_variable = { support_to_remove = support_counter }
		if = {
			limit = { has_country_flag = remove_PRL_support }
			subtract_from_variable = { LUX.PRL_support = support_to_remove }
			clr_country_flag = remove_PRL_support
		}
		if = {
			limit = { has_country_flag = remove_LSAP_support }
			subtract_from_variable = { LUX.LSAP_support = support_to_remove }
			clr_country_flag = remove_LSAP_support
		}
		if = {
			limit = { has_country_flag = remove_KPL_support }
			subtract_from_variable = { LUX.KPL_support = support_to_remove }
			clr_country_flag = remove_KPL_support
		}
	}
	else = {
		set_temp_variable = { temp_support_to_add = LUX.PD_support }
		set_variable = { LUX.PD_support = 0 }
		if = {
			limit = { check_variable = { PRL_support > 0 } }
			add_to_temp_variable = { temp_support_quotient = 1 }
		}
		if = {
			limit = { check_variable = { LSAP_support > 0 } }
			add_to_temp_variable = { temp_support_quotient = 1 }
		}
		if = {
			limit = { check_variable = { KPL_support > 0 } }
			add_to_temp_variable = { temp_support_quotient = 1 }
		}
		divide_temp_variable = { temp_support_to_add = temp_support_quotient }

		add_to_variable = { LUX.PRL_support = temp_support_to_add }
		add_to_variable = { LUX.LSAP_support = temp_support_to_add }
		add_to_variable = { LUX.KPL_support = temp_support_to_add }
	}

	set_variable = { LUX.PD_support_loc = LUX.PD_support }
	set_variable = { LUX.PRL_support_loc = LUX.PRL_support }
	set_variable = { LUX.LSAP_support_loc = LUX.LSAP_support }
	set_variable = { LUX.KPL_support_loc = LUX.KPL_support }
}
LUX_chamberofdeputies_remove_support_PD_50 = {
	set_temp_variable = { support_to_remove = 5.00 }
	if = {
		limit = { check_variable = { LUX.PD_support > support_to_remove } }
		add_to_variable = { LUX.PD_support = support_to_remove }
	
		if = {
			limit = { check_variable = { LUX.PRL_support > 4.999 } }
			add_to_temp_variable = { support_counter = 1 }					# 5.00 x 1
			set_country_flag = remove_PRL_support							# PRL_support = yes
			if = {
				limit = { check_variable = { LUX.LSAP_support > 2.499 } }
				add_to_temp_variable = { support_counter = 1 }				# 2.50 x 2
				set_country_flag = remove_LSAP_support						# PRL_support = yes; LSAP_support = yes
				if = {
					limit = { check_variable = { LUX.KPL_support > 1.667 } }
					add_to_temp_variable = { support_counter = 1 }			# 1.67 x 3
					set_country_flag = remove_KPL_support					# PRL_support = yes; LSAP_support = yes; KPL_support = yes
				}
			}
			else_if = {														# PRL_support = yes; LSAP_support = no
				limit = { check_variable = { LUX.KPL_support > 2.499 } }
				add_to_temp_variable = { support_counter = 1 }				# 2.50 x 2
				set_country_flag = remove_KPL_support						# PRL_support = yes; LSAP_support = no; KPL_support = yes
			}
		}
		else_if = {															# PRL_support = no
			limit = { check_variable = { LUX.LSAP_support > 4.999 } }
			add_to_temp_variable = { support_counter = 1 }					# 5.00 x 1
			set_country_flag = remove_LSAP_support							# PRL_support = no; LSAP_support = yes
			if = {
				limit = { check_variable = { LUX.KPL_support > 2.499 } }
				add_to_temp_variable = { support_counter = 1 }				# 2.50 x 2
				set_country_flag = remove_KPL_support						# PRL_support = no; LSAP_support = yes; KPL_support = yes
			}
		}
		else = {
			add_to_temp_variable = { support_counter = 1 }					# 5.00 x 1
			set_country_flag = remove_KPL_support							# PRL_support = no; LSAP_support = no; KPL_support = yes
		}
	
		divide_temp_variable = { support_to_remove = support_counter }
		if = {
			limit = { has_country_flag = remove_PRL_support }
			subtract_from_variable = { LUX.PRL_support = support_to_remove }
			clr_country_flag = remove_PRL_support
		}
		if = {
			limit = { has_country_flag = remove_LSAP_support }
			subtract_from_variable = { LUX.LSAP_support = support_to_remove }
			clr_country_flag = remove_LSAP_support
		}
		if = {
			limit = { has_country_flag = remove_KPL_support }
			subtract_from_variable = { LUX.KPL_support = support_to_remove }
			clr_country_flag = remove_KPL_support
		}
	}
	else = {
		set_temp_variable = { temp_support_to_add = LUX.PD_support }
		set_variable = { LUX.PD_support = 0 }
		if = {
			limit = { check_variable = { PRL_support > 0 } }
			add_to_temp_variable = { temp_support_quotient = 1 }
		}
		if = {
			limit = { check_variable = { LSAP_support > 0 } }
			add_to_temp_variable = { temp_support_quotient = 1 }
		}
		if = {
			limit = { check_variable = { KPL_support > 0 } }
			add_to_temp_variable = { temp_support_quotient = 1 }
		}
		divide_temp_variable = { temp_support_to_add = temp_support_quotient }

		add_to_variable = { LUX.PRL_support = temp_support_to_add }
		add_to_variable = { LUX.LSAP_support = temp_support_to_add }
		add_to_variable = { LUX.KPL_support = temp_support_to_add }
	}


	set_variable = { LUX.PD_support_loc = LUX.PD_support }
	set_variable = { LUX.PRL_support_loc = LUX.PRL_support }
	set_variable = { LUX.LSAP_support_loc = LUX.LSAP_support }
	set_variable = { LUX.KPL_support_loc = LUX.KPL_support }
}
LUX_chamberofdeputies_PD_small_decrease_effect = {
	subtract_from_variable = { LUX.PD_support = 10.00 }
	add_to_variable = { LUX.LSAP_support = 5.00 }
	LUX_chamberofdeputies_add_confidence_10 = yes

	if = {
		limit = { check_variable = { LUX.PRL_support > 2.499 } }
		add_to_temp_variable = { support_to_add = 2.5 }					# 2.50
		set_country_flag = remove_PRL_support							# PRL_support = yes
		if = {
			limit = { check_variable = { LUX.KPL_support > 2.499 } }
			add_to_temp_variable = { support_to_add = 2.5 }				# 5.00
			set_country_flag = remove_KPL_support						# PRL_support = yes; KPL_support = yes
		}
		else_if = {
			limit = { check_variable = { LUX.PRL_support > 4.999 } }
			add_to_temp_variable = { support_to_add = 2.5 }				# 5.00
		}																# PRL_support = yes; KPL_support = no; PRL_support = 2yes
		else = {
			clr_country_flag = remove_PRL_support
			add_to_variable = { LUX.PRL_support = 5.00 }				# 5.00
		}																# PRL_support = yes; KPL_support = no; PRL_support = 2no
	}
	else_if = {
		limit = { check_variable = { LUX.KPL_support > 4.999 } }
		add_to_temp_variable = { support_to_add = 5.00 }				# 5.00
		set_country_flag = remove_KPL_support							# PRL_support = no; KPL_support = yes
	}
	else = { add_to_variable = { LUX.LSAP_support = 5.00 } }			# 5.00
																		# PRL_support = no; KPL_support = no
	if = {
		limit = { has_country_flag = remove_PRL_support }
		subtract_from_variable = { LUX.PRL_support = support_to_remove }
		clr_country_flag = remove_PRL_support
	}
	if = {
		limit = { has_country_flag = remove_KPL_support }
		subtract_from_variable = { LUX.KPL_support = support_to_remove }
		clr_country_flag = remove_KPL_support
	}

	set_variable = { LUX.PD_support_loc = LUX.PD_support }
	set_variable = { LUX.PRL_support_loc = LUX.PRL_support }
	set_variable = { LUX.LSAP_support_loc = LUX.LSAP_support }
	set_variable = { LUX.KPL_support_loc = LUX.KPL_support }
}

LUX_chamberofdeputies_add_support_PRL_5 = {
	set_temp_variable = { support_to_remove = 0.5 }
	if = {
		limit = { check_variable = { LUX.PRL_support < 99.5 } }
		add_to_variable = { LUX.PRL_support = support_to_remove }
	
		if = {
			limit = { check_variable = { LUX.PD_support > 0.499 } }
			add_to_temp_variable = { support_counter = 1 }					# 0.50 x 1
			set_country_flag = remove_PD_support							# PD_support = yes
			if = {
				limit = { check_variable = { LUX.LSAP_support > 0.249 } }
				add_to_temp_variable = { support_counter = 1 }				# 0.25 x 2
				set_country_flag = remove_LSAP_support						# PD_support = yes; LSAP_support = yes
				if = {
					limit = { check_variable = { LUX.KPL_support > 0.166 } }
					add_to_temp_variable = { support_counter = 1 }			# 0.167 x 3
					set_country_flag = remove_KPL_support					# PD_support = yes; LSAP_support = yes; KPL_support = yes
				}
			}
			else_if = {														# PD_support = yes; LSAP_support = no
				limit = { check_variable = { LUX.KPL_support > 0.249 } }
				add_to_temp_variable = { support_counter = 1 }				# 0.25 x 2
				set_country_flag = remove_KPL_support						# PD_support = yes; LSAP_support = no; KPL_support = yes
			}
		}
		else_if = {															# PD_support = no
			limit = { check_variable = { LUX.LSAP_support > 0.499 } }
			add_to_temp_variable = { support_counter = 1 }					# 0.50 x 1
			set_country_flag = remove_LSAP_support							# PD_support = no; LSAP_support = yes
			if = {
				limit = { check_variable = { LUX.KPL_support > 0.249 } }
				add_to_temp_variable = { support_counter = 1 }				# 0.25 x 2
				set_country_flag = remove_KPL_support						# PD_support = no; LSAP_support = yes; KPL_support = yes
			}
		}
		else = {
			add_to_temp_variable = { support_counter = 1 }					# 0.50 x 1
			set_country_flag = remove_KPL_support							# PD_support = no; LSAP_support = no; KPL_support = yes
		}
	
		divide_temp_variable = { support_to_remove = support_counter }
		if = {
			limit = { has_country_flag = remove_PD_support }
			subtract_from_variable = { LUX.PD_support = support_to_remove }
			clr_country_flag = remove_PD_support
		}
		if = {
			limit = { has_country_flag = remove_LSAP_support }
			subtract_from_variable = { LUX.LSAP_support = support_to_remove }
			clr_country_flag = remove_LSAP_support
		}
		if = {
			limit = { has_country_flag = remove_KPL_support }
			subtract_from_variable = { LUX.KPL_support = support_to_remove }
			clr_country_flag = remove_KPL_support
		}
	}
	else = {
		set_variable = { LUX.PD_support = 0 }
		set_variable = { LUX.PRL_support = 100 }
		set_variable = { LUX.LSAP_support = 0 }
		set_variable = { LUX.KPL_support = 0 }
	}

	set_variable = { LUX.PD_support_loc = LUX.PD_support }
	set_variable = { LUX.PRL_support_loc = LUX.PRL_support }
	set_variable = { LUX.LSAP_support_loc = LUX.LSAP_support }
	set_variable = { LUX.KPL_support_loc = LUX.KPL_support }
}
LUX_chamberofdeputies_add_support_PRL_50 = {
	set_temp_variable = { support_to_remove = 5.00 }
	if = {
		limit = { check_variable = { LUX.PRL_support < 95 } }
		add_to_variable = { LUX.PRL_support = support_to_remove }
	
		if = {
			limit = { check_variable = { LUX.PD_support > 4.999 } }
			add_to_temp_variable = { support_counter = 1 }					# 5.00 x 1
			set_country_flag = remove_PD_support							# PD_support = yes
			if = {
				limit = { check_variable = { LUX.LSAP_support > 2.499 } }
				add_to_temp_variable = { support_counter = 1 }				# 2.50 x 2
				set_country_flag = remove_LSAP_support						# PD_support = yes; LSAP_support = yes
				if = {
					limit = { check_variable = { LUX.KPL_support > 1.667 } }
					add_to_temp_variable = { support_counter = 1 }			# 1.667 x 3
					set_country_flag = remove_KPL_support					# PD_support = yes; LSAP_support = yes; KPL_support = yes
				}
			}
			else_if = {														# PD_support = yes; LSAP_support = no
				limit = { check_variable = { LUX.KPL_support > 2.499 } }
				add_to_temp_variable = { support_counter = 1 }				# 2.50 x 2
				set_country_flag = remove_KPL_support						# PD_support = yes; LSAP_support = no; KPL_support = yes
			}
		}
		else_if = {															# PD_support = no
			limit = { check_variable = { LUX.LSAP_support > 4.999 } }
			add_to_temp_variable = { support_counter = 1 }					# 5.00 x 1
			set_country_flag = remove_LSAP_support							# PD_support = no; LSAP_support = yes
			if = {
				limit = { check_variable = { LUX.KPL_support > 2.499 } }
				add_to_temp_variable = { support_counter = 1 }				# 2.50 x 2
				set_country_flag = remove_KPL_support						# PD_support = no; LSAP_support = yes; KPL_support = yes
			}
		}
		else = {
			add_to_temp_variable = { support_counter = 1 }					# 5.00 x 1
			set_country_flag = remove_KPL_support							# PD_support = no; LSAP_support = no; KPL_support = yes
		}
	
		divide_temp_variable = { support_to_remove = support_counter }
		if = {
			limit = { has_country_flag = remove_PD_support }
			subtract_from_variable = { LUX.PD_support = support_to_remove }
			clr_country_flag = remove_PD_support
		}
		if = {
			limit = { has_country_flag = remove_LSAP_support }
			subtract_from_variable = { LUX.LSAP_support = support_to_remove }
			clr_country_flag = remove_LSAP_support
		}
		if = {
			limit = { has_country_flag = remove_KPL_support }
			subtract_from_variable = { LUX.KPL_support = support_to_remove }
			clr_country_flag = remove_KPL_support
		}
	}
	else = {
		set_variable = { LUX.PD_support = 0 }
		set_variable = { LUX.PRL_support = 100 }
		set_variable = { LUX.LSAP_support = 0 }
		set_variable = { LUX.KPL_support = 0 }
	}

	set_variable = { LUX.PD_support_loc = LUX.PD_support }
	set_variable = { LUX.PRL_support_loc = LUX.PRL_support }
	set_variable = { LUX.LSAP_support_loc = LUX.LSAP_support }
	set_variable = { LUX.KPL_support_loc = LUX.KPL_support }
}
LUX_chamberofdeputies_add_support_PRL_100 = {
	set_temp_variable = { support_to_remove = 10.00 }
	if = {
		limit = { check_variable = { LUX.PRL_support < 90 } }
		add_to_variable = { LUX.PRL_support = support_to_remove }
	
		if = {
			limit = { check_variable = { LUX.PD_support > 9.999 } }
			add_to_temp_variable = { support_counter = 1 }					# 10.00 x 1
			set_country_flag = remove_PD_support							# PD_support = yes
			if = {
				limit = { check_variable = { LUX.LSAP_support > 4.999 } }
				add_to_temp_variable = { support_counter = 1 }				# 5.00 x 2
				set_country_flag = remove_LSAP_support						# PD_support = yes; LSAP_support = yes
				if = {
					limit = { check_variable = { LUX.KPL_support > 3.333 } }
					add_to_temp_variable = { support_counter = 1 }			# 3.33 x 3
					set_country_flag = remove_KPL_support					# PD_support = yes; LSAP_support = yes; KPL_support = yes
				}
			}
			else_if = {														# PD_support = yes; LSAP_support = no
				limit = { check_variable = { LUX.KPL_support > 4.999 } }
				add_to_temp_variable = { support_counter = 1 }				# 5.00 x 2
				set_country_flag = remove_KPL_support						# PD_support = yes; LSAP_support = no; KPL_support = yes
			}
		}
		else_if = {															# PD_support = no
			limit = { check_variable = { LUX.LSAP_support > 9.999 } }
			add_to_temp_variable = { support_counter = 1 }					# 10.00 x 1
			set_country_flag = remove_LSAP_support							# PD_support = no; LSAP_support = yes
			if = {
				limit = { check_variable = { LUX.KPL_support > 4.999 } }
				add_to_temp_variable = { support_counter = 1 }				# 5.00 x 2
				set_country_flag = remove_KPL_support						# PD_support = no; LSAP_support = yes; KPL_support = yes
			}
		}
		else = {
			add_to_temp_variable = { support_counter = 1 }					# 10.00 x 1
			set_country_flag = remove_KPL_support							# PD_support = no; LSAP_support = no; KPL_support = yes
		}
	
		divide_temp_variable = { support_to_remove = support_counter }
		if = {
			limit = { has_country_flag = remove_PD_support }
			subtract_from_variable = { LUX.PRL_support = support_to_remove }
			clr_country_flag = remove_PD_support
		}
		if = {
			limit = { has_country_flag = remove_LSAP_support }
			subtract_from_variable = { LUX.LSAP_support = support_to_remove }
			clr_country_flag = remove_LSAP_support
		}
		if = {
			limit = { has_country_flag = remove_KPL_support }
			subtract_from_variable = { LUX.KPL_support = support_to_remove }
			clr_country_flag = remove_KPL_support
		}
	}
	else = {
		set_variable = { LUX.PD_support = 0 }
		set_variable = { LUX.PRL_support = 100 }
		set_variable = { LUX.LSAP_support = 0 }
		set_variable = { LUX.KPL_support = 0 }
	}

	set_variable = { LUX.PD_support_loc = LUX.PD_support }
	set_variable = { LUX.PRL_support_loc = LUX.PRL_support }
	set_variable = { LUX.LSAP_support_loc = LUX.LSAP_support }
	set_variable = { LUX.KPL_support_loc = LUX.KPL_support }
}

LUX_chamberofdeputies_remove_support_LSAP_50 = {
	set_temp_variable = { support_to_remove = 5.00 }
	if = {
		limit = { check_variable = { LUX.PRL_support > support_to_remove } }
		add_to_variable = { LUX.LSAP_support = support_to_remove }
	
		if = {
			limit = { check_variable = { LUX.PRL_support > 4.999 } }
			add_to_temp_variable = { support_counter = 1 }					# 5.00 x 1
			set_country_flag = remove_PRL_support							# PRL_support = yes
			if = {
				limit = { check_variable = { LUX.PD_support > 2.499 } }
				add_to_temp_variable = { support_counter = 1 }				# 2.50 x 2
				set_country_flag = remove_PD_support						# PRL_support = yes; PD_support = yes
				if = {
					limit = { check_variable = { LUX.KPL_support > 1.667 } }
					add_to_temp_variable = { support_counter = 1 }			# 1.67 x 3
					set_country_flag = remove_KPL_support					# PRL_support = yes; PD_support = yes; KPL_support = yes
				}
			}
			else_if = {														# PRL_support = yes; PD_support = no
				limit = { check_variable = { LUX.KPL_support > 2.499 } }
				add_to_temp_variable = { support_counter = 1 }				# 2.50 x 2
				set_country_flag = remove_KPL_support						# PRL_support = yes; PD_support = no; KPL_support = yes
			}
		}
		else_if = {															# PRL_support = no
			limit = { check_variable = { LUX.PD_support > 4.999 } }
			add_to_temp_variable = { support_counter = 1 }					# 5.00 x 1
			set_country_flag = remove_PD_support							# PRL_support = no; PD_support = yes
			if = {
				limit = { check_variable = { LUX.KPL_support > 2.499 } }
				add_to_temp_variable = { support_counter = 1 }				# 2.50 x 2
				set_country_flag = remove_KPL_support						# PRL_support = no; PD_support = yes; KPL_support = yes
			}
		}
		else = {
			add_to_temp_variable = { support_counter = 1 }					# 5.00 x 1
			set_country_flag = remove_KPL_support							# PRL_support = no; PD_support = no; KPL_support = yes
		}
	
		divide_temp_variable = { support_to_remove = support_counter }
		if = {
			limit = { has_country_flag = remove_PRL_support }
			subtract_from_variable = { LUX.PRL_support = support_to_remove }
			clr_country_flag = remove_PRL_support
		}
		if = {
			limit = { has_country_flag = remove_PD_support }
			subtract_from_variable = { LUX.PD_support = support_to_remove }
			clr_country_flag = remove_PD_support
		}
		if = {
			limit = { has_country_flag = remove_KPL_support }
			subtract_from_variable = { LUX.KPL_support = support_to_remove }
			clr_country_flag = remove_KPL_support
		}
	}
	else = {
		set_temp_variable = { temp_support_to_add = LUX.PRL_support }
		set_variable = { LUX.PRL_support = 0 }
		if = {
			limit = { check_variable = { PD_support > 0 } }
			add_to_temp_variable = { temp_support_quotient = 1 }
		}
		if = {
			limit = { check_variable = { LSAP_support > 0 } }
			add_to_temp_variable = { temp_support_quotient = 1 }
		}
		if = {
			limit = { check_variable = { KPL_support > 0 } }
			add_to_temp_variable = { temp_support_quotient = 1 }
		}
		divide_temp_variable = { temp_support_to_add = temp_support_quotient }

		add_to_variable = { LUX.PD_support = temp_support_to_add }
		add_to_variable = { LUX.LSAP_support = temp_support_to_add }
		add_to_variable = { LUX.KPL_support = temp_support_to_add }
	}

	set_variable = { LUX.PD_support_loc = LUX.PD_support }
	set_variable = { LUX.PRL_support_loc = LUX.PRL_support }
	set_variable = { LUX.LSAP_support_loc = LUX.LSAP_support }
	set_variable = { LUX.KPL_support_loc = LUX.KPL_support }
}
LUX_chamberofdeputies_add_support_LSAP_5 = {
	set_temp_variable = { support_to_remove = 0.5 }
	if = {
		limit = { check_variable = { LUX.LSAP_support < 99.5 } }
		add_to_variable = { LUX.LSAP_support = support_to_remove }
	
		if = {
			limit = { check_variable = { LUX.PD_support > 0.499 } }
			add_to_temp_variable = { support_counter = 1 }					# 0.50 x 1
			set_country_flag = remove_PD_support							# PD_support = yes
			if = {
				limit = { check_variable = { LUX.PRL_support > 0.249 } }
				add_to_temp_variable = { support_counter = 1 }				# 0.25 x 2
				set_country_flag = remove_PRL_support						# PD_support = yes; LSAP_support = yes
				if = {
					limit = { check_variable = { LUX.KPL_support > 0.166 } }
					add_to_temp_variable = { support_counter = 1 }			# 0.167 x 3
					set_country_flag = remove_KPL_support					# PD_support = yes; LSAP_support = yes; KPL_support = yes
				}
			}
			else_if = {														# PD_support = yes; LSAP_support = no
				limit = { check_variable = { LUX.KPL_support > 0.249 } }
				add_to_temp_variable = { support_counter = 1 }				# 0.25 x 2
				set_country_flag = remove_KPL_support						# PD_support = yes; LSAP_support = no; KPL_support = yes
			}
		}
		else_if = {															# PD_support = no
			limit = { check_variable = { LUX.PRL_support > 0.499 } }
			add_to_temp_variable = { support_counter = 1 }					# 0.50 x 1
			set_country_flag = remove_PRL_support							# PD_support = no; LSAP_support = yes
			if = {
				limit = { check_variable = { LUX.KPL_support > 0.249 } }
				add_to_temp_variable = { support_counter = 1 }				# 0.25 x 2
				set_country_flag = remove_KPL_support						# PD_support = no; LSAP_support = yes; KPL_support = yes
			}
		}
		else = {
			add_to_temp_variable = { support_counter = 1 }					# 0.50 x 1
			set_country_flag = remove_KPL_support							# PD_support = no; LSAP_support = no; KPL_support = yes
		}
	
		divide_temp_variable = { support_to_remove = support_counter }
		if = {
			limit = { has_country_flag = remove_PD_support }
			subtract_from_variable = { LUX.PD_support = support_to_remove }
			clr_country_flag = remove_PD_support
		}
		if = {
			limit = { has_country_flag = remove_PRL_support }
			subtract_from_variable = { LUX.PRL_support = support_to_remove }
			clr_country_flag = remove_PRL_support
		}
		if = {
			limit = { has_country_flag = remove_KPL_support }
			subtract_from_variable = { LUX.KPL_support = support_to_remove }
			clr_country_flag = remove_KPL_support
		}
	}
	else = {
		set_variable = { LUX.PD_support = 0 }
		set_variable = { LUX.PRL_support = 0 }
		set_variable = { LUX.LSAP_support = 100 }
		set_variable = { LUX.KPL_support = 0 }
	}

	set_variable = { LUX.PD_support_loc = LUX.PD_support }
	set_variable = { LUX.PRL_support_loc = LUX.PRL_support }
	set_variable = { LUX.LSAP_support_loc = LUX.LSAP_support }
	set_variable = { LUX.KPL_support_loc = LUX.KPL_support }
}
LUX_chamberofdeputies_add_support_LSAP_15 = {
	set_temp_variable = { support_to_remove = 1.5 }
	if = {
		limit = { check_variable = { LUX.LSAP_support < 98.5 } }
		add_to_variable = { LUX.LSAP_support = support_to_remove }
	
		if = {
			limit = { check_variable = { LUX.PD_support > 1.499 } }
			add_to_temp_variable = { support_counter = 1 }					# 1.50 x 1
			set_country_flag = remove_PD_support							# PD_support = yes
			if = {
				limit = { check_variable = { LUX.PRL_support > 0.749 } }
				add_to_temp_variable = { support_counter = 1 }				# 0.75 x 2
				set_country_flag = remove_PRL_support						# PD_support = yes; LSAP_support = yes
				if = {
					limit = { check_variable = { LUX.KPL_support > 0.499 } }
					add_to_temp_variable = { support_counter = 1 }			# 0.50 x 3
					set_country_flag = remove_KPL_support					# PD_support = yes; LSAP_support = yes; KPL_support = yes
				}
			}
			else_if = {														# PD_support = yes; LSAP_support = no
				limit = { check_variable = { LUX.KPL_support > 0.749 } }
				add_to_temp_variable = { support_counter = 1 }				# 0.75 x 2
				set_country_flag = remove_KPL_support						# PD_support = yes; LSAP_support = no; KPL_support = yes
			}
		}
		else_if = {															# PD_support = no
			limit = { check_variable = { LUX.PRL_support > 1.499 } }
			add_to_temp_variable = { support_counter = 1 }					# 1.50 x 1
			set_country_flag = remove_PRL_support							# PD_support = no; LSAP_support = yes
			if = {
				limit = { check_variable = { LUX.KPL_support > 0.749 } }
				add_to_temp_variable = { support_counter = 1 }				# 0.75 x 2
				set_country_flag = remove_KPL_support						# PD_support = no; LSAP_support = yes; KPL_support = yes
			}
		}
		else = {
			add_to_temp_variable = { support_counter = 1 }					# 1.50 x 1
			set_country_flag = remove_KPL_support							# PD_support = no; LSAP_support = no; KPL_support = yes
		}
	
		divide_temp_variable = { support_to_remove = support_counter }
		if = {
			limit = { has_country_flag = remove_PD_support }
			subtract_from_variable = { LUX.PD_support = support_to_remove }
			clr_country_flag = remove_PD_support
		}
		if = {
			limit = { has_country_flag = remove_PRL_support }
			subtract_from_variable = { LUX.PRL_support = support_to_remove }
			clr_country_flag = remove_PRL_support
		}
		if = {
			limit = { has_country_flag = remove_KPL_support }
			subtract_from_variable = { LUX.KPL_support = support_to_remove }
			clr_country_flag = remove_KPL_support
		}
	}
	else = {
		set_variable = { LUX.PD_support = 0 }
		set_variable = { LUX.PRL_support = 0 }
		set_variable = { LUX.LSAP_support = 100 }
		set_variable = { LUX.KPL_support = 0 }
	}

	set_variable = { LUX.PD_support_loc = LUX.PD_support }
	set_variable = { LUX.PRL_support_loc = LUX.PRL_support }
	set_variable = { LUX.LSAP_support_loc = LUX.LSAP_support }
	set_variable = { LUX.KPL_support_loc = LUX.KPL_support }
}

LUX_chamberofdeputies_remove_support_KPL_50 = {
	set_temp_variable = { support_to_remove = 5.00 }
	if = {
		limit = { check_variable = { KPL_support > support_to_remove } }
		add_to_variable = { LUX.KPL_support = support_to_remove }
	
		if = {
			limit = { check_variable = { LUX.PRL_support > 4.999 } }
			add_to_temp_variable = { support_counter = 1 }					# 5.00 x 1
			set_country_flag = remove_PRL_support							# PRL_support = yes
			if = {
				limit = { check_variable = { LUX.PD_support > 2.499 } }
				add_to_temp_variable = { support_counter = 1 }				# 2.50 x 2
				set_country_flag = remove_PD_support						# PRL_support = yes; PD_support = yes
				if = {
					limit = { check_variable = { LUX.LSAP_support > 1.667 } }
					add_to_temp_variable = { support_counter = 1 }			# 1.67 x 3
					set_country_flag = remove_LSAP_support					# PRL_support = yes; PD_support = yes; LSAP_support = yes
				}
			}
			else_if = {														# PRL_support = yes; PD_support = no
				limit = { check_variable = { LUX.LSAP_support > 2.499 } }
				add_to_temp_variable = { support_counter = 1 }				# 2.50 x 2
				set_country_flag = remove_LSAP_support						# PRL_support = yes; PD_support = no; LSAP_support = yes
			}
		}
		else_if = {															# PRL_support = no
			limit = { check_variable = { LUX.PD_support > 4.999 } }
			add_to_temp_variable = { support_counter = 1 }					# 5.00 x 1
			set_country_flag = remove_PD_support							# PRL_support = no; PD_support = yes
			if = {
				limit = { check_variable = { LUX.LSAP_support > 2.499 } }
				add_to_temp_variable = { support_counter = 1 }				# 2.50 x 2
				set_country_flag = remove_LSAP_support						# PRL_support = no; PD_support = yes; LSAP_support = yes
			}
		}
		else = {
			add_to_temp_variable = { support_counter = 1 }					# 5.00 x 1
			set_country_flag = remove_LSAP_support							# PRL_support = no; PD_support = no; LSAP_support = yes
		}
	
		divide_temp_variable = { support_to_remove = support_counter }
		if = {
			limit = { has_country_flag = remove_PRL_support }
			subtract_from_variable = { LUX.PRL_support = support_to_remove }
			clr_country_flag = remove_PRL_support
		}
		if = {
			limit = { has_country_flag = remove_PD_support }
			subtract_from_variable = { LUX.PD_support = support_to_remove }
			clr_country_flag = remove_PD_support
		}
		if = {
			limit = { has_country_flag = remove_LSAP_support }
			subtract_from_variable = { LUX.KPL_support = support_to_remove }
			clr_country_flag = remove_LSAP_support
		}
	}
	else = {
		set_temp_variable = { temp_support_to_add = LUX.KPL_support }
		set_variable = { LUX.KPL_support = 0 }
		if = {
			limit = { check_variable = { PD_support > 0 } }
			add_to_temp_variable = { temp_support_quotient = 1 }
		}
		if = {
			limit = { check_variable = { LSAP_support > 0 } }
			add_to_temp_variable = { temp_support_quotient = 1 }
		}
		if = {
			limit = { check_variable = { PRL_support > 0 } }
			add_to_temp_variable = { temp_support_quotient = 1 }
		}
		divide_temp_variable = { temp_support_to_add = temp_support_quotient }

		add_to_variable = { LUX.PD_support = temp_support_to_add }
		add_to_variable = { LUX.LSAP_support = temp_support_to_add }
		add_to_variable = { LUX.PRL_support = temp_support_to_add }
	}

	set_variable = { LUX.PD_support_loc = LUX.PD_support }
	set_variable = { LUX.PRL_support_loc = LUX.PRL_support }
	set_variable = { LUX.LSAP_support_loc = LUX.LSAP_support }
	set_variable = { LUX.KPL_support_loc = LUX.KPL_support }
}
LUX_chamberofdeputies_add_support_KPL_25 = {
	set_temp_variable = { support_to_remove = 2.50 }
	if = {
		limit = { check_variable = { LUX.KPL_support < 97.5 } }
		add_to_variable = { LUX.KPL_support = support_to_remove }
	
		if = {
			limit = { check_variable = { LUX.PD_support > 2.499 } }
			add_to_temp_variable = { support_counter = 1 }					# 2.50 x 1
			set_country_flag = remove_PD_support							# PD_support = yes
			if = {
				limit = { check_variable = { LUX.LSAP_support > 1.249 } }
				add_to_temp_variable = { support_counter = 1 }				# 1.25 x 2
				set_country_flag = remove_LSAP_support						# PD_support = yes; LSAP_support = yes
				if = {
					limit = { check_variable = { LUX.PRL_support > 0.832 } }
					add_to_temp_variable = { support_counter = 1 }			# 0.833 x 3
					set_country_flag = remove_PRL_support					# PD_support = yes; LSAP_support = yes; PRL_support = yes
				}
			}
			else_if = {														# PD_support = yes; LSAP_support = no
				limit = { check_variable = { LUX.PRL_support > 1.249 } }
				add_to_temp_variable = { support_counter = 1 }				# 1.25 x 2
				set_country_flag = remove_PRL_support						# PD_support = yes; LSAP_support = no; PRL_support = yes
			}
		}
		else_if = {															# PD_support = no
			limit = { check_variable = { LUX.LSAP_support > 2.499 } }
			add_to_temp_variable = { support_counter = 1 }					# 2.50 x 1
			set_country_flag = remove_LSAP_support							# PD_support = no; LSAP_support = yes
			if = {
				limit = { check_variable = { LUX.PRL_support > 1.249 } }
				add_to_temp_variable = { support_counter = 1 }				# 1.25 x 2
				set_country_flag = remove_PRL_support						# PD_support = no; LSAP_support = yes; PRL_support = yes
			}
		}
		else = {
			add_to_temp_variable = { support_counter = 1 }					# 2.50 x 1
			set_country_flag = remove_PRL_support							# PD_support = no; LSAP_support = no; PRL_support = yes
		}
	
		divide_temp_variable = { support_to_remove = support_counter }
		if = {
			limit = { has_country_flag = remove_PD_support }
			subtract_from_variable = { LUX.PD_support = support_to_remove }
			clr_country_flag = remove_PD_support
		}
		if = {
			limit = { has_country_flag = remove_LSAP_support }
			subtract_from_variable = { LUX.LSAP_support = support_to_remove }
			clr_country_flag = remove_LSAP_support
		}
		if = {
			limit = { has_country_flag = remove_PRL_support }
			subtract_from_variable = { LUX.PRL_support = support_to_remove }
			clr_country_flag = remove_PRL_support
		}
	}
	else = {
		set_variable = { LUX.PD_support = 0 }
		set_variable = { LUX.PRL_support = 0 }
		set_variable = { LUX.LSAP_support = 0 }
		set_variable = { LUX.KPL_support = 100 }
	}

	set_variable = { LUX.PD_support_loc = LUX.PD_support }
	set_variable = { LUX.PRL_support_loc = LUX.PRL_support }
	set_variable = { LUX.LSAP_support_loc = LUX.LSAP_support }
	set_variable = { LUX.KPL_support_loc = LUX.KPL_support }
}
LUX_chamberofdeputies_add_support_KPL_100 = {
	set_temp_variable = { support_to_remove = 10.00 }
	if = {
		limit = { check_variable = { LUX.KPL_support < 90 } }
		add_to_variable = { LUX.KPL_support = support_to_remove }
	
		if = {
			limit = { check_variable = { LUX.PD_support > 9.999 } }
			add_to_temp_variable = { support_counter = 1 }					# 10.00 x 1
			set_country_flag = remove_PD_support							# PD_support = yes
			if = {
				limit = { check_variable = { LUX.LSAP_support > 4.999 } }
				add_to_temp_variable = { support_counter = 1 }				# 5.00 x 2
				set_country_flag = remove_LSAP_support						# PD_support = yes; LSAP_support = yes
				if = {
					limit = { check_variable = { LUX.PRL_support > 3.332 } }
					add_to_temp_variable = { support_counter = 1 }			# 3.333 x 3
					set_country_flag = remove_PRL_support					# PD_support = yes; LSAP_support = yes; PRL_support = yes
				}
			}
			else_if = {														# PD_support = yes; LSAP_support = no
				limit = { check_variable = { LUX.PRL_support > 4.999 } }
				add_to_temp_variable = { support_counter = 1 }				# 5.00 x 2
				set_country_flag = remove_PRL_support						# PD_support = yes; LSAP_support = no; PRL_support = yes
			}
		}
		else_if = {															# PD_support = no
			limit = { check_variable = { LUX.LSAP_support > 9.999 } }
			add_to_temp_variable = { support_counter = 1 }					# 10.00 x 1
			set_country_flag = remove_LSAP_support							# PD_support = no; LSAP_support = yes
			if = {
				limit = { check_variable = { LUX.PRL_support > 4.99 } }
				add_to_temp_variable = { support_counter = 1 }				# 5.00 x 2
				set_country_flag = remove_PRL_support						# PD_support = no; LSAP_support = yes; PRL_support = yes
			}
		}
		else = {
			add_to_temp_variable = { support_counter = 1 }					# 10.00 x 1
			set_country_flag = remove_PRL_support							# PD_support = no; LSAP_support = no; PRL_support = yes
		}

		divide_temp_variable = { support_to_remove = support_counter }
		if = {
			limit = { has_country_flag = remove_PD_support }
			subtract_from_variable = { LUX.PD_support = support_to_remove }
			clr_country_flag = remove_PD_support
		}
		if = {
			limit = { has_country_flag = remove_LSAP_support }
			subtract_from_variable = { LUX.LSAP_support = support_to_remove }
			clr_country_flag = remove_LSAP_support
		}
		if = {
			limit = { has_country_flag = remove_PRL_support }
			subtract_from_variable = { LUX.PRL_support = support_to_remove }
			clr_country_flag = remove_PRL_support
		}
	}
	else = {
		set_variable = { LUX.PD_support = 0 }
		set_variable = { LUX.PRL_support = 0 }
		set_variable = { LUX.LSAP_support = 0 }
		set_variable = { LUX.KPL_support = 100 }
	}

	set_variable = { LUX.PD_support_loc = LUX.PD_support }
	set_variable = { LUX.PRL_support_loc = LUX.PRL_support }
	set_variable = { LUX.LSAP_support_loc = LUX.LSAP_support }
	set_variable = { LUX.KPL_support_loc = LUX.KPL_support }
}
