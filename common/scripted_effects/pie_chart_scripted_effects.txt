# Scripted GUI Pie Chart example by Yard1
# Feel free to use in your mods, but give credits to Yard1 (both in code, with comments; and on your download page)

# this creates a pie chart of popularities - can be easily adapted to show anything that adds up to 100%

create_pie_chart = {
	clear_array = pie_chart
	resize_array = {
		array = pie_chart
		value = 1 # 1 is value for neutrality frame, so we don't have to make a loop for it
		size = 100 # 100 pieces, each percent is one piece
	}

	set_variable = { num_of_seats = NRAF_seats }
	add_to_variable = { num_of_seats = DNVP_seats }
	add_to_variable = { num_of_seats = DVP_seats }
	add_to_variable = { num_of_seats = DSTP_seats }
	add_to_variable = { num_of_seats = Zentrum_seats }
	add_to_variable = { num_of_seats = SAPD_seats }
	add_to_variable = { num_of_seats = SPD_seats }
	add_to_variable = { num_of_seats = KPD_seats }
	divide_variable = { num_of_seats = 100 }

	set_temp_variable = { nraf_pop = NRAF_seats }
	divide_temp_variable = { nraf_pop = num_of_seats }
	round_temp_variable = nraf_pop
	set_variable = { ROOT.last_idx = nraf_pop }

	set_temp_variable = { dnvp_pop = DNVP_seats }
	divide_temp_variable = { dnvp_pop = num_of_seats }
	round_temp_variable = dnvp_pop
	add_to_temp_variable = { dnvp_pop = ROOT.last_idx }
	for_loop_effect = {
		start = ROOT.last_idx
		end = dnvp_pop
		set_variable = { pie_chart^v = 2 }
		add_to_variable = { ROOT.last_idx = 1 }
	}

	set_temp_variable = { dvp_pop = DVP_seats }
	divide_temp_variable = { dvp_pop = num_of_seats }
	round_temp_variable = dvp_pop
	add_to_temp_variable = { dvp_pop = ROOT.last_idx }
	for_loop_effect = {
		start = ROOT.last_idx
		end = dvp_pop
		set_variable = { pie_chart^v = 3 }
		add_to_variable = { ROOT.last_idx = 1 }
	}

	set_temp_variable = { dstp_pop = DSTP_seats }
	divide_temp_variable = { dstp_pop = num_of_seats }
	round_temp_variable = dstp_pop
	add_to_temp_variable = { dstp_pop = ROOT.last_idx }
	for_loop_effect = {
		start = ROOT.last_idx
		end = dstp_pop
		set_variable = { pie_chart^v = 4 }
		add_to_variable = { ROOT.last_idx = 1 }
	}

	set_temp_variable = { zentrum_pop = Zentrum_seats }
	divide_temp_variable = { zentrum_pop = num_of_seats }
	round_temp_variable = zentrum_pop
	add_to_temp_variable = { zentrum_pop = ROOT.last_idx }
	for_loop_effect = {
		start = ROOT.last_idx
		end = zentrum_pop
		set_variable = { pie_chart^v = 5 }
		add_to_variable = { ROOT.last_idx = 1 }
	}

	set_temp_variable = { sapd_pop = SAPD_seats }
	divide_temp_variable = { sapd_pop = num_of_seats }
	round_temp_variable = sapd_pop
	add_to_temp_variable = { sapd_pop = ROOT.last_idx }
	for_loop_effect = {
		start = ROOT.last_idx
		end = sapd_pop
		set_variable = { pie_chart^v = 6 }
		add_to_variable = { ROOT.last_idx = 1 }
	}

	set_temp_variable = { spd_pop = SPD_seats }
	divide_temp_variable = { spd_pop = num_of_seats }
	round_temp_variable = spd_pop
	add_to_temp_variable = { spd_pop = ROOT.last_idx }
	for_loop_effect = {
		start = ROOT.last_idx
		end = spd_pop
		set_variable = { pie_chart^v = 7 }
		add_to_variable = { ROOT.last_idx = 1 }
	}

	set_temp_variable = { kpd_pop = KPD_seats }
	divide_temp_variable = { kpd_pop = num_of_seats }
	round_temp_variable = kpd_pop
	add_to_temp_variable = { kpd_pop = ROOT.last_idx }
	for_loop_effect = {
		start = ROOT.last_idx
		end = kpd_pop
		set_variable = { pie_chart^v = 8 }
	}

	clear_variable = ROOT.last_idx
	add_to_variable = { pie_chart_dirty = 1 }
}