# Scripted GUI Pie Chart example by Yard1
# Feel free to use in your mods, but give credits to Yard1 (both in code, with comments; and on your download page)

# this creates a pie chart of popularities - can be easily adapted to show anything that adds up to 100%

create_pie_chart = { #credits for base code to Yard1
	clear_array = pie_chart
	resize_array = {
		array = pie_chart
		value = 1 # 1 is value for neutrality frame, so we don't have to make a loop for it
		size = 100 # 100 pieces, each percent is one piece
	}

	set_temp_variable = { nraf_pop = NRAF_seats_f }
	divide_temp_variable = { nraf_pop = GER_total_seats }
	multiply_temp_variable = { nraf_pop = 100 }
	round_temp_variable = nraf_pop
	set_variable = { ROOT.last_idx = nraf_pop }

	set_temp_variable = { dnvp_pop = DNVP_seats_f }
	divide_temp_variable = { dnvp_pop = GER_total_seats }
	multiply_temp_variable = { dnvp_pop = 100 }
	round_temp_variable = dnvp_pop
	add_to_temp_variable = { dnvp_pop = ROOT.last_idx }
	for_loop_effect = {
		start = ROOT.last_idx
		end = dnvp_pop
		set_variable = { pie_chart^v = 2 }
		add_to_variable = { ROOT.last_idx = 1 }
	}

	set_temp_variable = { dvu_pop = DVU_seats_f }
	divide_temp_variable = { dvu_pop = GER_total_seats }
	multiply_temp_variable = { dvu_pop = 100 }
	round_temp_variable = dvu_pop
	add_to_temp_variable = { dvu_pop = ROOT.last_idx }
	for_loop_effect = {
		start = ROOT.last_idx
		end = dvu_pop
		set_variable = { pie_chart^v = 4 }
		add_to_variable = { ROOT.last_idx = 1 }
	}

	set_temp_variable = { zentrum_pop = Zentrum_seats_f }
	divide_temp_variable = { zentrum_pop = GER_total_seats }
	multiply_temp_variable = { zentrum_pop = 100 }
	round_temp_variable = zentrum_pop
	add_to_temp_variable = { zentrum_pop = ROOT.last_idx }
	for_loop_effect = {
		start = ROOT.last_idx
		end = zentrum_pop
		set_variable = { pie_chart^v = 5 }
		add_to_variable = { ROOT.last_idx = 1 }
	}

	set_temp_variable = { dstp_pop = DSTP_seats_f }
	divide_temp_variable = { dstp_pop = GER_total_seats }
	multiply_temp_variable = { dstp_pop = 100 }
	round_temp_variable = dstp_pop
	add_to_temp_variable = { dstp_pop = ROOT.last_idx }
	for_loop_effect = {
		start = ROOT.last_idx
		end = dstp_pop
		set_variable = { pie_chart^v = 6 }
		add_to_variable = { ROOT.last_idx = 1 }
	}

	set_temp_variable = { spd_pop = SPD_seats_f }
	divide_temp_variable = { spd_pop = GER_total_seats }
	multiply_temp_variable = { spd_pop = 100 }
	round_temp_variable = spd_pop
	add_to_temp_variable = { spd_pop = ROOT.last_idx }
	for_loop_effect = {
		start = ROOT.last_idx
		end = spd_pop
		set_variable = { pie_chart^v = 7 }
		add_to_variable = { ROOT.last_idx = 1 }
	}

	set_temp_variable = { sapd_pop = SAPD_seats_f }
	divide_temp_variable = { sapd_pop = GER_total_seats }
	multiply_temp_variable = { sapd_pop = 100 }
	round_temp_variable = sapd_pop
	add_to_temp_variable = { sapd_pop = ROOT.last_idx }
	for_loop_effect = {
		start = ROOT.last_idx
		end = sapd_pop
		set_variable = { pie_chart^v = 8 }
		add_to_variable = { ROOT.last_idx = 1 }
	}

	set_temp_variable = { kpd_pop = KPD_seats_f }
	divide_temp_variable = { kpd_pop = GER_total_seats }
	multiply_temp_variable = { kpd_pop = 100 }
	round_temp_variable = kpd_pop
	add_to_temp_variable = { kpd_pop = ROOT.last_idx }
	for_loop_effect = {
		start = ROOT.last_idx
		end = kpd_pop
		set_variable = { pie_chart^v = 9 }
	}

	clear_variable = ROOT.last_idx
	add_to_variable = { pie_chart_dirty = 1 }
}
GER_undo_gov = {
	IF = {
		limit = { has_completed_focus = GER_nraf_in_coalition}
		uncomplete_national_focus = {
			focus = GER_nraf_in_coalition
			uncomplete_children = no
			refund_political_power = no
		}
	}
	IF = {
		limit = { has_completed_focus = GER_dnvp_in_coalition}
		uncomplete_national_focus = {
			focus = GER_dnvp_in_coalition
			uncomplete_children = no
			refund_political_power = no
		}
	}
	IF = {
		limit = { has_completed_focus = GER_dvu_in_coalition}
		uncomplete_national_focus = {
			focus = GER_dvu_in_coalition
			uncomplete_children = no
			refund_political_power = no
		}
	}
	IF = {
		limit = { has_completed_focus = GER_zentrum_in_coalition}
		uncomplete_national_focus = {
			focus = GER_zentrum_in_coalition
			uncomplete_children = no
			refund_political_power = no
		}
	}
	IF = {
		limit = { has_completed_focus = GER_dstp_in_coalition}
		uncomplete_national_focus = {
			focus = GER_dstp_in_coalition
			uncomplete_children = no
			refund_political_power = no
		}
	}
	IF = {
		limit = { has_completed_focus = GER_spd_in_coalition}
		uncomplete_national_focus = {
			focus = GER_spd_in_coalition
			uncomplete_children = no
			refund_political_power = no
		}
	}
	IF = {
		limit = { has_completed_focus = GER_kpd_in_coalition}
		uncomplete_national_focus = {
			focus = GER_kpd_in_coalition
			uncomplete_children = no
			refund_political_power = no
		}
	}
	set_variable = { GER_gov_seats = 0 }
	set_variable = { GER_chosen_gov_leader = 0 }
	set_variable = { GER_gov_leader_seats = 0 }
	set_variable = { GER_coalition_cost = 0 }
	set_country_flag = GER_no_gov
	clr_country_flag = GER_nraf_coa
	clr_country_flag = GER_dnvp_coa
	clr_country_flag = GER_dvu_coa
	clr_country_flag = GER_dstp_coa
	clr_country_flag = GER_zentrum_coa
	clr_country_flag = GER_spd_coa
	clr_country_flag = GER_kpd_coa
	create_pie_chart = yes
	add_to_variable = { pie_chart_dirty = 1 }
}
GER_set_seats = {
	IF = {
		limit = {
			NOT = { has_completed_focus = GER_demokratieschutzgesetz}
			NOT = { has_completed_focus = GER_ban_the_reactionaries }
		}
		set_variable = { NRAF_seats_f = NRAF_seats }
	}
	IF = {
		limit = {
			NOT = { has_country_flag = GER_dvu_formed }
			NOT = { has_completed_focus = GER_ban_the_reactionaries }
		}
		set_variable = { DNVP_seats_f = DNVP_seats }
	}
	IF = {
		limit = {
			has_country_flag = GER_dvu_formed
			NOT = { has_completed_focus = GER_ban_the_reactionaries }
		}
		set_variable = { DVU_seats_f = DVU_seats }
	}
	IF = {
		limit = {
			NOT = { has_country_flag = GER_dvu_formed }
			NOT = { has_completed_focus = GER_ban_the_reactionaries }
		}
		set_variable = { Zentrum_seats_f = Zentrum_seats }
	}
	IF = {
		limit = {
			NOT = { has_completed_focus = GER_ban_the_reactionaries }
		}
		set_variable = { DSTP_seats_f = DSTP_seats }
	}
	IF = {
		limit = { 
			NOT = { has_completed_focus = GER_a_ban_for_the_spd}
			NOT = { has_completed_focus = GER_form_the_sed }
		}
		set_variable = { SPD_seats_f = SPD_seats }
	}
	IF = {
		limit = {
			NOT = { has_completed_focus = GER_reinstate_the_reichtagsbrandverordnung}
			OR = { 
				has_completed_focus = GER_legalize_the_remaining_communist_parties
				has_completed_focus = GER_unite_with_the_sapd_resistance
			}
		}
		set_variable = { SAPD_seats_f = SAPD_seats }
	}
	IF = {
		limit = {
			NOT = { has_completed_focus = GER_reinstate_the_reichtagsbrandverordnung}
			OR = { 
				has_completed_focus = GER_legalize_the_remaining_communist_parties
				has_completed_focus = GER_comintern_kpd_funding
			}
		}
		set_variable = { KPD_seats_f = KPD_seats }
	}
}

GER_set_parliament = {
	set_variable = { GER_total_seats = NRAF_seats_f }
	IF = {
		limit = { 
			OR = {
				has_completed_focus = GER_demokratieschutzgesetz
				has_completed_focus = GER_ban_the_reactionaries 
			}
		}
		set_variable = { GER_total_seats = 0 }
	}
	IF = {
		limit = {
			NOT = { has_country_flag = GER_dvu_formed }
			NOT = { has_completed_focus = GER_ban_the_reactionaries }
		}
		add_to_variable = { GER_total_seats = DNVP_seats_f }
	}
	IF = {
		limit = {
			has_country_flag = GER_dvu_formed
			NOT = { has_completed_focus = GER_ban_the_reactionaries }
		}
		add_to_variable = { GER_total_seats = DVU_seats_f }
	}
	IF = {
		limit = {	
			NOT = { has_completed_focus = GER_ban_the_reactionaries }
			
		}
		add_to_variable = { GER_total_seats = DSTP_seats_f }
	}
	IF = {
		limit = {
			NOT = { has_country_flag = GER_dvu_formed }
			NOT = { has_completed_focus = GER_ban_the_reactionaries }
		}
		add_to_variable = { GER_total_seats = Zentrum_seats_f }
	}
	IF = {
		limit = {
			NOT = { has_completed_focus = GER_a_ban_for_the_spd}
			NOT = { has_completed_focus = GER_form_the_sed }
		}
		add_to_variable = { GER_total_seats = SPD_seats_f }
	}
	IF = {
		limit = {
			NOT = { has_completed_focus = GER_reinstate_the_reichtagsbrandverordnung}
			OR = { 
				has_completed_focus = GER_legalize_the_remaining_communist_parties
				has_completed_focus = GER_unite_with_the_sapd_resistance
			}
		}
		add_to_variable = { GER_total_seats = SAPD_seats_f}
	}
	IF = {
		limit = {
			NOT = { has_completed_focus = GER_reinstate_the_reichtagsbrandverordnung}
			OR = { 
				has_completed_focus = GER_legalize_the_remaining_communist_parties
				has_completed_focus = GER_comintern_kpd_funding
			}
		}
		add_to_variable = { GER_total_seats = KPD_seats_f}
	}
	set_variable = { GER_majority_seats = GER_total_seats }
	set_variable = { GER_2_thirds = GER_total_seats}
	multiply_variable = { GER_2_thirds = 0.66 }
	round_variable = GER_2_thirds
	divide_variable = { GER_majority_seats = 2 }
	round_variable = GER_majority_seats
	add_to_variable = { GER_majority_seats = 1 }

}

GER_collapse_dvu = {
	custom_effect_tooltip = GER_collapse_dvu_tt
	IF = {
		limit = {
			has_completed_focus = GER_knock_stegerwald_down_a_peg
		}
		set_variable = { DNVP_seats_f = DVU_seats_f }
		multiply_variable = { DNVP_seats_f = 0.7 }
		round_variable = DNVP_seats_f
		set_variable = { DNVP_seats = DVU_seats }
		multiply_variable = { DNVP_seats = 0.6 }
		round_variable = DNVP_seats
	}
	else_if = {
		limit = { has_completed_focus = GER_utilize_the_zentrum_freeze }
		set_variable = { DNVP_seats_f = DVU_seats_f }
		multiply_variable = { DNVP_seats_f = 0.6 }
		round_variable = DNVP_seats_f
		set_variable = { DNVP_seats = DVU_seats }
		multiply_variable = { DNVP_seats = 0.55 }
		round_variable = DNVP_seats
	}
	else = {
		set_variable = { DNVP_seats_f = DVU_seats_f }
		multiply_variable = { DNVP_seats_f = 0.5 }
		round_variable = DNVP_seats_f
		set_variable = { DNVP_seats = DVU_seats }
		multiply_variable = { DNVP_seats = 0.5 }
		round_variable = DNVP_seats
	}
	set_variable = { Zentrum_seats_f = DVU_seats_f }
	subtract_from_variable = { Zentrum_seats_f = DNVP_seats_f }
	set_variable = { Zentrum_seats = DVU_seats }
	subtract_from_variable = { Zentrum_seats = DNVP_seats}
	set_variable = { DVU_seats = 0 }
	set_variable = { DVU_seats_f = 0 }
	clr_country_flag = GER_dvu_formed
	create_pie_chart = yes
	add_to_variable = { pie_chart_dirty = 1 }
	#todo make this break any CVU gov
}
GER_monarchist_bop_absurdly_high_increase_effect = {
	add_power_balance_value = {
		id = GER_monarchist_power_balance
		value = 1
	}
}

GER_monarchist_bop_very_high_increase_effect = {
	add_power_balance_value = {
		id = GER_monarchist_power_balance
		value = 0.25
	}
}

GER_monarchist_bop_high_increase_effect = {
	add_power_balance_value = {
		id = GER_monarchist_power_balance
		value = 0.2
	}
}

GER_monarchist_bop_medium_increase_effect = {
	add_power_balance_value = {
		id = GER_monarchist_power_balance
		value = 0.15
	}
}

GER_monarchist_bop_low_increase_effect = {
	add_power_balance_value = {
		id = GER_monarchist_power_balance
		value = 0.1
	}
}

GER_monarchist_bop_very_low_increase_effect = {
	add_power_balance_value = {
		id = GER_monarchist_power_balance
		value = 0.05
	}
}

GER_monarchist_bop_absurdly_high_decrease_effect = {
	add_power_balance_value = {
		id = GER_monarchist_power_balance
		value = -1
	}
}

GER_monarchist_bop_very_high_decrease_effect = {
	add_power_balance_value = {
		id = GER_monarchist_power_balance
		value = -0.25
	}
}

GER_monarchist_bop_high_decrease_effect = {
	add_power_balance_value = {
		id = GER_monarchist_power_balance
		value = -0.2
	}
}

GER_monarchist_bop_medium_decrease_effect = {
	add_power_balance_value = {
		id = GER_monarchist_power_balance
		value = -0.15
	}
}

GER_monarchist_bop_low_decrease_effect = {
	add_power_balance_value = {
		id = GER_monarchist_power_balance
		value = -0.1
	}
}

GER_monarchist_bop_very_low_decrease_effect = {
	add_power_balance_value = {
		id = GER_monarchist_power_balance
		value = -0.05
	}
}

#MEFO BILLS SHENANIGANS
GER_mefo_bills_level_up = {
	add_to_variable = {
		GER_mefo_bills_political_power_cost = 0.15
		tooltip = GER_mefo_bills_increase_pp_cost_effect_tt
	}
}

GER_mefo_bills_level_down = {
	add_to_variable = {
		GER_mefo_bills_political_power_cost = -0.15
		tooltip = GER_mefo_bills_decrease_pp_cost_effect_tt
	}
}

#GER_remove_mefo_bills = {		#defunct
	#remove_dynamic_modifier = { modifier = GER_mefo_bills_modifier }
#}

r56_GER_ersatz_idea_upgrade = { 
	if = {
		limit = {
			has_idea = r56_ger_ersatz_idea_3
		}
		swap_ideas = {
			remove_idea = r56_ger_ersatz_idea_3
			add_idea = r56_ger_ersatz_idea_4
		}
	}
	if = {
		limit = {
			has_idea = r56_ger_ersatz_idea_2
		}
		swap_ideas = {
			remove_idea = r56_ger_ersatz_idea_2
			add_idea = r56_ger_ersatz_idea_3
		}
	}
	if = {
		limit = {
			has_idea = r56_ger_ersatz_idea_1
		}
		swap_ideas = {
			remove_idea = r56_ger_ersatz_idea_1
			add_idea = r56_ger_ersatz_idea_2
		}
	}
	if = {
		limit = {
			NOT = {
				OR = {
					has_idea = r56_ger_ersatz_idea_1
					has_idea = r56_ger_ersatz_idea_2
					has_idea = r56_ger_ersatz_idea_3
					has_idea = r56_ger_ersatz_idea_4
				}
			}
		}
		add_ideas = r56_ger_ersatz_idea_1
	}
}