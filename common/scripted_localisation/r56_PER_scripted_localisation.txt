# Allows you to create your own dynamic keys
# to be called in localization.
# defined_text -> this is it, we're defining the text
# text -> a discrete entry that can be picked to display in loc.
# trigger -> determines if a text entry will be picked or not.
# (The triggers need to be valid for the scope the key is called in
# (eg Root or From.From).)
# localization_key -> points to the localization key
# that'll be used if trigger passes


defined_text = { # Country
	name = GetGermanSupport_PER_10_relations
	text = {
		trigger = {
			check_variable  = {
				var = per_alignment_ger
				value = 10
				compare = greater_than
			}
		}
		localization_key = GER_PER_10_relations
	}
	text = {
		localization_key = GER_PER_10_relations_blocked
	}
}


defined_text = { # Country
	name = GetGermanSupport_PER_20_relations
	text = {
		trigger = {
			check_variable  = {
				var = per_alignment_ger
				value = 20
				compare = greater_than
			}
		}
		localization_key = GER_PER_20_relations
	}
	text = {
		localization_key = GER_PER_20_relations_blocked
	}
}

defined_text = { # Country
	name = GetGermanSupport_PER_30_relations
	text = {
		trigger = {
			check_variable  = {
				var = per_alignment_ger
				value = 30
				compare = greater_than
			}
		}
		localization_key = GER_PER_30_relations
	}
	text = {
		localization_key = GER_PER_30_relations_blocked
	}
}

defined_text = { # Country
	name = GetGermanSupport_PER_40_relations
	text = {
		trigger = {
			check_variable  = {
				var = per_alignment_ger
				value = 40
				compare = greater_than
			}
		}
		localization_key = GER_PER_40_relations
	}
	text = {
		localization_key = GER_PER_40_relations_blocked
	}
}


#britbongs exist

defined_text = { # Country
	name = GetBritishSupport_PER_30_relations
	text = {
		trigger = {
			check_variable  = {
				var = per_alignment_bri
				value = 30
				compare = greater_than
			}
		}
		localization_key = BRI_PER_30_relations
	}
	text = {
		localization_key = BRI_PER_30_relations_blocked
	}
}

























