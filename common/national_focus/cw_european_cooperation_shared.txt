joint_focus = {
    id = EUR_treaty_of_dunkirk
    
    icon = GFX_goal_generic_army_doctrines

    x = 20
    y = 0
    cost = 5

    available = {
        if = {
            limit = {
                original_tag = FRA
            }
            NOT = {
                has_war_with = ENG
            }
        }
        if = {
            limit = {
                original_tag = ENG
            }
            NOT = {
                has_war_with = FRA
            }
        }
    }

    search_filters = {}
    
    completion_reward_joint_originator = {
        if = {
            limit = {
                original_tag = FRA
            }
            ENG = {
                #country_event =
            }
        }
        if = {
            limit = {
                original_tag = ENG
            }
            FRA = {
                #country_event =
            }
        }
    }
}
joint_focus = {
    id = EUR_treaty_of_brussel
    
    icon = GFX_goal_generic_army_doctrines

    prerequisite = {
        focus = EUR_treaty_of_dunkirk
    }

    x = 0
    y = 1
    relative_position_id = EUR_treaty_of_dunkirk
    cost = 5

    available = {
        if = {
            limit = {
                original_tag = FRA
            }
            NOT = {
                has_war_with = ENG
            }
        }
        if = {
            limit = {
                original_tag = ENG
            }
            NOT = {
                has_war_with = FRA
            }
        }
    }

    bypass = {
        OR = {
            is_faction_leader = yes
            if = {
                limit = {
                    original_tag = FRA
                }
                is_in_faction_with = ENG
            }
            if = {
                limit = {
                    original_tag = ENG
                }
                is_in_faction_with = FRA
            }
        }
    }

    search_filters = {}

    completion_reward_joint_originator = {
        create_faction = "Western Union"
        HOL = {
            country_event = generic.5
        }
        BEL = {
            country_event = generic.5
        }
        LUX = {
            country_event = generic.5
        }
        if = {
            limit = {
                original_tag = FRA
            }
            ENG = {
                country_event = generic.5
            }
        }
        if = {
            limit = {
                original_tag = ENG
            }
            FRA = {
                country_event = generic.5
            }
        }
    }
}