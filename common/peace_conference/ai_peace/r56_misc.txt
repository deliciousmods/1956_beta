peace_ai_desires = {
	SOV_give_sweden_scandinavia = {
		peace_action_type = take_states
		enable = {
			is_historical_focus_on = yes
			is_major = yes
			ROOT = { 
				ROOT = { tag = SOV }
				has_government = communism 
				is_in_faction_with = SWE 
				SWE = { has_government = communism } 
			}
			ROOT.FROM = { tag = SWE }
			ROOT.FROM.FROM = { OR = { tag = NOR tag = DEN tag = ICE } }
			ROOT.FROM.FROM.FROM = { OR = { is_core_of = ROOT.FROM.FROM } }
		}
		ai_desire = -1000
	}
	SOV_dont_puppet_scandinavia = { #Don't steal land from Communist Sweden if you're in the same faction.
		peace_action_type = puppet
		enable = {
			is_historical_focus_on = yes
			is_major = yes
			ROOT = { 
				ROOT = { tag = SOV }
				has_government = communism 
				is_in_faction_with = SWE 
				SWE = { has_government = communism } 
			}
			ROOT.FROM = { tag = SWE }
			ROOT.FROM.FROM = { OR = { tag = NOR tag = DEN tag = ICE } }
		}
		ai_desire = -1000
	}
}