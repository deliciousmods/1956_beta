PRC_bolshevik_r56 = {
	name = "Communist China alternate plan: pro-Soviet"
	desc = "Alternate behavior for Communist China"
	allowed = {
		original_tag = PRC
		NOT = { has_dlc = "No Compromise, No Surrender" }
	}
	abort = { always = no }
	enable = {
		has_game_rule = {
			rule = PRC_ai_behavior
			option = ALTERNATE_1
		}
	}

	ai_national_focuses = {	#To-do, if anyone wants to make a basic AI path for this, it would be appreciated.
	}

	focus_factors = {
		PRC_sov_cooperation = 99
	}

	# Keep small, as it is used as a factor for some things (such as research needs)
	# Recommended around 1.0. Useful for relation between plans
	weight = {
		factor = 1.0
		modifier = {
			factor = 1.0
		}
	}
}