on_actions = {
    on_startup = {
        effect = {
            AST = {
                remove_ideas = AST_R56_economy_placeholder
            }
        }
    }
	on_state_control_changed = {
	
		effect = {
				if = {
			
					if = {
						limit = {
							FROM = { tag = AST } 
							NOT = { ROOT = { tag = AST } }
							NOT = { FROM = { has_country_flag = AST_lost_725 } }
						}
						FROM = {
							set_country_flag = AST_lost_725
							add_to_variable = { AST_industry_local_resources_factor = -0.1 }
						}
					}
			
					if = {
						limit = {
							ROOT = { tag = AST }
							NOT = { FROM = { tag = AST } }
							ROOT = { has_country_flag = AST_lost_725 } 
						}
						ROOT = {
							clr_country_flag = AST_lost_725
							add_to_variable = { AST_industry_local_resources_factor = 0.1 }
						}
					}
				}
		}
	}
		
}
