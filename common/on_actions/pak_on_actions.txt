#Cause problems (mainly in vpi scripted effects
on_actions = {
    on_daily = {
		effect = {
			if = {
				limit = {
					tag = PAK
					owns_state = 430
				}
				if = {
					limit = {
						NOT = { has_country_flag = 1inf }
					}
					
				}		
			}
        }
    }
}