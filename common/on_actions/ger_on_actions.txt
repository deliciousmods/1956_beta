on_actions = {
	
	on_declare_war = {
		effect = {
			#Resources gained from the Deal with the Soviet Union get removed
			#if = {
			#	limit = {
			#		FROM = { original_tag = SOV }
			#		tag = GER
			#		has_global_flag = ger_sov_accepts_agreement
			#	}
			#	GER = {
			#		country_event = r56_germany_resource_removal.1
			#	}
			#}
		}
	}

	#on_new_term_election = {
	#	effect = {
	#		if = {
	#			limit = { tag = GER }
	#			country_event = r56_germany_elections.1
	#		}
	#	}
	#}
	
	on_startup = {
		effect = {
			GER = {
				every_state = {
					limit = {
						is_core_of = GER
					}
					remove_core_of = NAZ
				}
			}
		}
	}
	
	on_offer_join_faction = {
		effect = {
			if = {
				limit = {
					original_tag = GER
					has_government = democratic
					is_faction_leader = yes
					has_completed_focus = GER_formalize_the_union
					has_completed_focus = GER_economic_integration
				}
				if = {
					limit = {
						FROM = {
							capital_scope = {
								is_on_continent = europe
							}
						}
					}
					FROM = {
						add_ideas = GER_european_customs_union
					}
				}
			}
		}
	}
	
	on_leave_faction = {
		effect = {
			if = {
				limit = { 
					has_idea = GER_european_customs_union
					GER = {
						is_faction_leader = yes
						has_government = democratic
					}
					NOT = { is_in_faction_with = GER }
				}
				remove_ideas = GER_european_customs_union
			}
		}
	}
	
	on_government_change = {
		effect = {
			if = {
				limit = {
					original_tag = GER
					has_government = communism
				}
				set_cosmetic_tag = GER_communist
			}
			if = {
				limit = {
					original_tag = GER
					NOT = { has_government = communism }
					has_cosmetic_tag = GER_communist
				}
				drop_cosmetic_tag = yes
			}
			
			#if = {
			#	limit = {
			#		original_tag = GER
			#		has_government = democratic
			#	}
			#	set_cosmetic_tag = GER_democratic
			#}
			#if = {
			#	limit = {
			#		original_tag = GER
			#		NOT = { has_government = democratic }
			#		has_cosmetic_tag = GER_democratic
			#	}
			#	drop_cosmetic_tag = yes
			#}
		}
	}
	
	#ROOT is winner #FROM gets annexed - This will also fire on_annex
	on_civil_war_end = {
		effect = {
			#End of German Civil War
			if = {
				limit = {
					FROM = {
						original_tag = GER
						has_government = fascism
					}
					original_tag = GER
					OR = {
						has_government = neutrality
						has_government = democratic
						has_government = communism
					}
					NOT = { is_subject_of = HOL }
				}
				add_ideas = GER_nazi_employment_sham
			}
			if = {
				limit = {
					FROM = {
						original_tag = GER
						has_government = fascism
					}
					original_tag = GER
					OR = {
						has_government = democratic
						has_government = communism
					}
					NOT = { is_subject_of = HOL }
				}
				country_event = { id = r56_germany_civil_war.3 days = 1 }
			}
		}
	}
}