# befriend
# conquer
# antagonize
# build_ship
# build_army
# unit_ratio
# build_building
# research_tech
# garrison
# protect
# influence
# send_volunteers_desire
# pp_spend_amount
# pp_spend_priority

AFG_build_army = {
	allowed = {
		original_tag = AFG
	}
	enable = {
		has_completed_focus = AFG_first_loya_jirga
		has_country_flag = AFG_seventh_loya_jirga_mission
		NOT = {
			power_balance_value = {
				id = AFG_power_balance
				value > 0.4
			}
		}
	}
	abort = {
		NOT = {
			has_country_flag = AFG_seventh_loya_jirga_mission
		}
	}

	ai_strategy = {
		type = force_build_armies
		value = 300
	}
}

AFG_save_pp_for_roles = {
	allowed = {
		original_tag = AFG
	}
	enable = {
		has_completed_focus = AFG_first_loya_jirga
		has_active_mission = AFG_first_loya_jirga_mission
		has_country_flag = AFG_win_loya_jirga
		NOT = {
			has_idea = r56i_laws_gender_traditional
			power_balance_value = {
				id = AFG_power_balance
				value > 0.45
			}
		}
	}

	abort = {
		has_idea = r56i_laws_gender_traditional
	}

	ai_strategy = {
		type = pp_spend_amount
		id = idea
		value = 150
	}
}