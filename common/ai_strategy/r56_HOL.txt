# befriend
# conquer
# antagonize
# build_ship
# build_army
# unit_ratio
# build_building
# research_tech
# garrison
# protect
# influence

HOL_dont_call_allies_to_indonesia_war_r56 = {
	allowed = {
		OR = { #basically all of the allies (ignoring subjects bcuz without overlord they cant join anyways lel)
			original_tag = ENG
			original_tag = USA
			original_tag = FRA
			original_tag = BEL
			original_tag = LUX
			original_tag = MEX
			original_tag = POL
			original_tag = GRE
			original_tag = YUG
			original_tag = NOR
			original_tag = BRA
			original_tag = HON
			original_tag = ROM #because you sometimes like to join the allies for whatever reason
			original_tag = CZE #just incase
			original_tag = ETH #especially not you
		}
	}

	enable = {
		INS = {
			exists = yes
			is_subject = no
			has_war_with = HOL
			is_in_faction = no
		}
		is_in_faction_with = HOL
	}

	abort_when_not_enabled = yes

	ai_strategy = {
		type = dont_join_wars_with
		id = "HOL"
		value = 100
	}

	ai_strategy = {
		type = diplo_action_acceptance
		id = "HOL"
		value = -100
		target = join_allies
	}

	ai_strategy = {
		type = ignore
		id = "HOL"
		value = 10
	}
}
