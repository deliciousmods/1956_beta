# befriend
# conquer
# antagonize
# build_ship
# build_army
# unit_ratio
# build_building
# research_tech
# garrison
# protect
# influence

JAP_its_always_time_to_eat_you_MEN = {
	enable = {
		original_tag = JAP
		MEN = {
			exists = yes
			is_puppet = no
			is_ai = yes
			is_in_faction = no
		}
		JAP = {
			exists = yes
			strength_ratio = {
				tag = MEN
				ratio > 1
			}
		}
		OR = {
			is_neighbor_of = MEN
			MAN = {
				is_neighbor_of = MEN
				OR = {
					is_puppet_of = JAP
					is_in_faction_with = JAP
				}
			}
		}
		NOT = {
			has_war_together_with = MEN
		}
	}

	abort_when_not_enabled = yes

	ai_strategy = {
		type = befriend
		id = "MEN"			
		value = -1000
	}

	ai_strategy = {
		type = alliance
		id = "MEN"			
		value = -1000 #Kick them from Faction
	}

	ai_strategy = {
		type = conquer
		id = "MEN"			
		value = 1000 
	}

	ai_strategy = {
		type = antagonize
		id = "MEN"			
		value = 1000 
	}

	ai_strategy = {
		type = declare_war
		id = "MEN"			
		value = 250
	}

}