political_actions = {
	# Restore Order to the Interior
	# Should be removed once more Omani / Muscati content comes around - Katsura!
	OMA_restore_order_in_the_interior  = {
		priority = 100
		icon = GFX_decision_oppression
		ai_will_do = { base = 500 }
		allowed = {  original_tag = OMA }

		available = {
			1102 = {
				compliance > 70
				is_owned_and_controlled_by = ROOT
				NOT = { is_core_of = ROOT }
			}
		}

		state_target = yes
		on_map_mode = map_and_decisions_view
		targets = { 1102 }
		target_trigger = { always = yes }

		visible = { 
			NOT = { 1102 = { is_core_of = ROOT } }
			NOT = { has_country_flag = OMA_restored_interior_omani_order }
		}

		cost = 75
		days_remove = 35  

		modifier = {
			weekly_manpower = -25
			stability_weekly = -0.003
			political_power_gain = -0.15
		}

		remove_effect = {
			IF = {
				limit = { 1102 = { is_claimed_by = ROOT } }
				1102 = { remove_claim_by = ROOT }
			}

			1102 = { add_core_of = ROOT set_demilitarized_zone = no }

			custom_effect_tooltip = generic_skip_one_line_tt
			
			add_war_support = 0.05
			add_stability = 0.05

			hidden_effect = { set_country_flag = OMA_restored_interior_omani_order }
		}

	}
	
	OMA_integrate_khasab = {
		allowed = { original_tag = OMA }
		icon = generic_nationalism

		available = {
			979 = {
				compliance > 70
				is_owned_by = ROOT
				is_fully_controlled_by = ROOT
			}
		}
		
		cost = 25
		days_remove = 35
		fire_only_once = yes
		visible = { 979 = { NOT = { is_core_of = OMA } } }

		highlight_states = { highlight_state_targets = { state = 979 } } # Khasab

		remove_effect = { 979 = { add_core_of = ROOT } }	
		ai_will_do = { factor = 50 }
	}

	# Establish the Sultanate of Oman 
	OMA_establih_the_sultanate_of_oman  = {
		priority = 90
		icon = GFX_decision_eng_trade_unions_support

		ai_will_do = {
			base = 1
			modifier = {
				factor = 0
				is_historical_focus_on = yes # A few more decades until we should be able to do this
			}
			modifier = {
				factor = 0.5
				date < 1943.1.1
			}
		}

		allowed = { original_tag = OMA }

		available = {
			has_government = neutrality
			is_subject = no # R56
			1102 = {
				is_core_of = ROOT
				is_owned_and_controlled_by = ROOT
			}
		}

		fire_only_once = yes
		cost = 100

		complete_effect = { 
			add_ideas = OMA_modernization_initiatives_idea  
			custom_effect_tooltip = generic_skip_one_line_tt
			set_cosmetic_tag = OMA_modern_tag  
		}
	}
}