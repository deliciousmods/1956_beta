political_actions = {
	# Restore Order to the Interior
	# Should be removed once more Omani / Muscati content comes around - Katsura!
	OMA_restore_order_in_the_interior  = {

		priority = 100

		icon = GFX_decision_oppression

		ai_will_do = {
			base = 500
		}

		allowed = {  
			original_tag = OMA
		}

		available = {
			ROOT = { has_idea = r56i_laws_leadership_divine }
			1102 = {
				compliance > 70
				is_owned_and_controlled_by = ROOT
				NOT = {
					is_core_of = ROOT
				}
			}
		}

		state_target = yes
		on_map_mode = map_and_decisions_view
		targets = { 1102 }
		target_trigger = {
			always = yes
		}

		visible = { 
			NOT = {
				1102 = {
					is_core_of = ROOT
				}
			}
			NOT = {
				has_country_flag = OMA_restored_interior_omani_order
			}
		}

		cost = 75
 
		days_remove = 35  

		modifier = {
			weekly_manpower = -25
			stability_weekly = -0.003
			political_power_gain = -0.15
		}

		complete_effect = { 

			IF = {
				limit = {
					1102 = {
						is_claimed_by = ROOT
					}
				}
				1102 = {
					remove_claim_by = ROOT
				}
			}

			1102 = {
				add_core_of = ROOT
			}

			custom_effect_tooltip = generic_skip_one_line_tt
			
			add_war_support = 0.05
			add_stability = 0.05

			hidden_effect = {
				set_country_flag = OMA_restored_interior_omani_order
			}
		}

		remove_effect = {

		}

	}
	
	OMA_integrate_khasab = {

		allowed = {
			original_tag = OMA
		}

		icon = generic_nationalism

		available = {
			979 = {
				compliance > 70
				is_owned_by = ROOT
				is_fully_controlled_by = ROOT
			}
		}
		
		cost = 25
		days_remove = 35
		fire_only_once = yes
		visible = {
			tag = OMA
			979 = {
				NOT = {
					is_core_of = OMA
				}
			}
		}

		highlight_states = { 
			highlight_state_targets = {
				state = 979 # Khasab
			}
		}

		complete_effect = {
		}

		remove_effect = {
			979 = {
				add_core_of = ROOT
			}	
		}	
		ai_will_do = {
			factor = 50
		}
	}
}