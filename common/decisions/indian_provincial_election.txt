indian_provincial_election = {
	dummy123 = {
		cost = 5
		icon = generic
	}
	congress_majority = {

		icon = congress
		on_map_mode = map_only
		available = {
			always = no
		}
		state_target = yes
		visible = {
			FROM = {
				NOT = {
					has_state_flag = princely_state
					state = 424
					state = 431
					state = 445
				}
				is_owned_by = RAJ
				is_core_of = RAJ
				INC_Majority = yes
			}
		}
		target_trigger = {
			FROM = {
				is_owned_by = RAJ
				is_core_of = RAJ
			}
		}
		days_remove = 0

		fire_only_once = no

		cost = 0

		modifier = {
			
		}

		complete_effect = {
			
		}
	}
	united_party_majority = {

		icon = united_party
		on_map_mode = map_only
		available = {
			always = no
		}
		state_target = yes
		visible = {
			FROM = {
				NOT = {
					has_state_flag = princely_state
					state = 424
					state = 431
					state = 445
				}
				is_owned_by = RAJ
				is_core_of = RAJ
				UPA_Majority = yes
			}
		}
		target_trigger = {
			FROM = {
				is_owned_by = RAJ
				is_core_of = RAJ
			}
		}
		days_remove = 0

		fire_only_once = no

		cost = 0

		modifier = {
			
		}

		complete_effect = {
			
		}
	}
	all_india_forward_bloc_majority = {

		icon = india_forward
		on_map_mode = map_only
		available = {
			always = no
		}
		state_target = yes
		visible = {
			FROM = {
				NOT = {
					has_state_flag = princely_state
					state = 424
					state = 431
					state = 445
				}
				is_owned_by = RAJ
				is_core_of = RAJ
				IFB_Majority = yes
			}
		}
		target_trigger = {
			FROM = {
				is_owned_by = RAJ
				is_core_of = RAJ
			}
		}
		days_remove = 0

		fire_only_once = no

		cost = 0

		modifier = {
			
		}

		complete_effect = {
			
		}
	}
	neutral_stalemate = {

		icon = generic
		on_map_mode = map_only
		available = {
			always = no
		}
		state_target = yes
		visible = {
			FROM = {
				NOT = {
					has_state_flag = princely_state
					state = 424
					state = 431
					state = 445
				}
				is_owned_by = RAJ
				is_core_of = RAJ
				Neutral_Stalemate = yes
			}
		}
		target_trigger = {
			FROM = {
				is_owned_by = RAJ
				is_core_of = RAJ
			}
		}
		days_remove = 0

		fire_only_once = no

		cost = 0

		modifier = {
			
		}

		complete_effect = {
			
		}
	}
	
	increase_local_influence = {
		icon = generic_nationalism
		on_map_mode = map_only
		state_target = yes
		cost = 30
		available = {
			NOT = { RAJ = { has_country_flag = running_influence } }
		}
		complete_effect = {
			RAJ = { set_country_flag = running_influence }
		}
		remove_effect = {
			RAJ = { clr_country_flag = running_influence }
			raj_increase_selected_influence_by_1 = yes
			raj_calculate_election = yes
		}
		days_remove = 45
		visible = {
			FROM = {
				NOT = {
					has_state_flag = princely_state
					has_state_flag = enemy_campaign_running
					state = 424
					state = 431
					state = 445
				}
				is_owned_by = RAJ
				is_core_of = RAJ
			}
		}
		target_trigger = {
			FROM = {
				is_owned_by = RAJ
				is_core_of = RAJ
			}
		}
	}
	
	inc_enemy_operation = {
		icon = generic_nationalism
		on_map_mode = map_and_decisions_view
		state_target = yes
		available = {
			always = no
		}
		activation = {
			always = no
		}
		highlight_state_targets = {
			state = var:enemy_campaign_state
		}
		is_good = no
		timeout_effect = {
			var:enemy_campaign_state = { add_to_variable = { inc = 1 } }
			RAJ = { clr_country_flag = inc_operation }
			var:enemy_campaign_state = { 
				clr_state_flag = enemy_campaign_running 
				clr_state_flag = raj_enemy_op 
				clr_state_flag = enemy_campaign_running
			}
			every_owned_state = {
				clr_state_flag = enemy_campaign_running
			}	
			set_variable = {
				enemy_campaign_state = 0
			}
			raj_calculate_election = yes
		}
		remove_days = 0
		days_mission_timeout = 30
		visible = {
			
		}
		target_trigger = {
			FROM = {
				NOT = {
					has_state_flag = princely_state
					has_state_flag = enemy_campaign_running
					state = 424
					state = 431
					state = 445
				}
				is_owned_by = RAJ
				is_core_of = RAJ
				
			}
			FROM = {
				has_state_flag = raj_enemy_op
			}
			RAJ = { has_country_flag = inc_operation }
		}
	}
	upa_enemy_operation = {
		icon = generic_nationalism
		on_map_mode = map_and_decisions_view
		state_target = yes
		available = {
			always = no
		}
		highlight_state_targets = {
			state = var:enemy_campaign_state
		}
		activation = {
			always = no
		}
		is_good = no
		timeout_effect = {
			var:enemy_campaign_state = { add_to_variable = { upa = 1 } }
			RAJ = { clr_country_flag = upa_operation }
			var:enemy_campaign_state = { 
				clr_state_flag = enemy_campaign_running 
				clr_state_flag = raj_enemy_op 
				clr_state_flag = enemy_campaign_running
			}
			every_owned_state = {
				clr_state_flag = enemy_campaign_running
			}	
			set_variable = {
				enemy_campaign_state = 0
			}
			raj_calculate_election = yes
		}
		days_mission_timeout = 30
		visible = {
		}
		target_trigger = {
			FROM = {
				NOT = {
					has_state_flag = princely_state
					has_state_flag = enemy_campaign_running
					state = 424
					state = 431
					state = 445
				}
				is_owned_by = RAJ
				is_core_of = RAJ
			}
			FROM = {
				has_state_flag = raj_enemy_op
			}
			RAJ = { has_country_flag = upa_operation }
		}
	}
	ifb_enemy_operation = {
		icon = generic_nationalism
		on_map_mode = map_and_decisions_view
		state_target = yes
		highlight_state_targets = {
			state = var:enemy_campaign_state
		}
		available = {
			always = no
		}
		activation = {
			always = no
		}
		is_good = no
		timeout_effect = {
			var:enemy_campaign_state = { add_to_variable = { ifb = 1 } }
			RAJ = { clr_country_flag = ifb_operation }
			var:enemy_campaign_state = { 
				clr_state_flag = enemy_campaign_running 
				clr_state_flag = raj_enemy_op 
				clr_state_flag = enemy_campaign_running
			}
			every_owned_state = {
				clr_state_flag = enemy_campaign_running
			}	
			set_variable = {
				enemy_campaign_state = 0
			}
			raj_calculate_election = yes
			
		}
		days_mission_timeout = 30
		visible = {
			
		}
		target_trigger = {
			FROM = {
				NOT = {
					has_state_flag = princely_state
					has_state_flag = enemy_campaign_running
					state = 424
					state = 431
					state = 445
				}
				is_owned_by = RAJ
				is_core_of = RAJ
			}
			FROM = {
				has_state_flag = raj_enemy_op
			}
			RAJ = { has_country_flag = ifb_operation }
		}
	}
}	