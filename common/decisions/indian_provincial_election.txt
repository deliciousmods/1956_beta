 
 ####################################
 #  INC - Indian National Congress  #
 #     UP/UPA - Unionist Party      #
 # IFB/ILP Independent Labour Party #
 ####################################

indian_provincial_election = {
	RAJ_provincial_election_countdown = {

		priority = 10
		
		icon = generic_political_discourse

		available = {
			hidden_trigger = { always = no }
		}

		days_mission_timeout = 365

		fire_only_once = yes

		activation = {
			has_country_flag = election_enabled
		}
		visible = {
			has_country_flag = election_enabled
		}
		cancel_if_not_visible = no

		is_good = yes

		timeout_effect = {
			custom_effect_tooltip = "The Election concludes"
			set_country_flag = raj_election_over
			clr_country_flag = election_enabled
			country_event = {
				id = india.4
				hours = 12
			}
		}
	}
	congress_majority = {

		icon = india_forward
		on_map_mode = map_only
		available = {
			always = no
		}
		state_target = yes
		visible = {
			FROM = {
				NOT = {
					has_state_flag = princely_state
					state = 424
					state = 431
					state = 445
				}
				is_owned_by = RAJ
				is_core_of = RAJ
				INC_Majority = yes
			}
		}
		target_trigger = {
			FROM = {
				is_owned_by = RAJ
				is_core_of = RAJ
			}
		}
		days_remove = 0

		fire_only_once = no

		cost = 0

		modifier = {
			
		}

		complete_effect = {
			
		}
	}
	united_party_majority = {

		icon = congress
		on_map_mode = map_only
		available = {
			always = no
		}
		state_target = yes
		visible = {
			FROM = {
				NOT = {
					has_state_flag = princely_state
					state = 424
					state = 431
					state = 445
				}
				is_owned_by = RAJ
				is_core_of = RAJ
				UPA_Majority = yes
			}
		}
		target_trigger = {
			FROM = {
				is_owned_by = RAJ
				is_core_of = RAJ
			}
		}
		days_remove = 0

		fire_only_once = no

		cost = 0

		modifier = {
			
		}

		complete_effect = {
			
		}
	}
	all_india_forward_bloc_majority = {

		icon = united_party
		on_map_mode = map_only
		available = {
			always = no
		}
		state_target = yes
		visible = {
			FROM = {
				NOT = {
					has_state_flag = princely_state
					state = 424
					state = 431
					state = 445
				}
				is_owned_by = RAJ
				is_core_of = RAJ
				IFB_Majority = yes
			}
		}
		target_trigger = {
			FROM = {
				is_owned_by = RAJ
				is_core_of = RAJ
			}
		}
		days_remove = 0

		fire_only_once = no

		cost = 0

		modifier = {
			
		}

		complete_effect = {
			
		}
	}
	neutral_stalemate = {

		icon = generic_scales
		on_map_mode = map_only
		available = {
			always = no
		}
		state_target = yes
		visible = {
			FROM = {
				NOT = {
					has_state_flag = princely_state
					state = 424
					state = 431
					state = 445
				}
				is_owned_by = RAJ
				is_core_of = RAJ
				Neutral_Stalemate = yes
			}
		}
		target_trigger = {
			FROM = {
				is_owned_by = RAJ
				is_core_of = RAJ
			}
		}
		days_remove = 0

		fire_only_once = no

		cost = 0

		modifier = {
			
		}

		complete_effect = {
			
		}
	}
	
	increase_local_influence = {
		icon = generic_political_rally
		on_map_mode = map_only
		state_target = yes
		cost = 60
		available = {
			NOT = { RAJ = { has_country_flag = running_influence } }
			has_completed_focus = RAJ_Provincial_Elections
		}
		complete_effect = {
			RAJ = { set_country_flag = running_influence }
			FROM = {
				set_state_flag = player_running
			}
		}
		remove_effect = {
			RAJ = { clr_country_flag = running_influence }
			FROM = { raj_increase_selected_influence_by_1 = yes }
			raj_calculate_election = yes
			FROM = {
				clr_state_flag = player_running
			}
		}
		ai_will_do = {
			factor = 100
			modifier = {
				factor = 0.1
				FROM = {
					UPA_Majority = yes
				}
				has_country_flag = upa_support
			}
			modifier = {
				factor = 0.1
				FROM = {
					INC_Majority = yes
				}
				has_country_flag = inc_support
			}
			modifier = {
				factor = 0.1
				FROM = {
					IFB_Majority = yes
				}
				has_country_flag = ifb_support
			}
			modifier = {
				factor = 3
				FROM = {
					Neutral_Stalemate = yes
				}
			}
		}
		days_remove = 30
		visible = {
			FROM = {
				NOT = {
					has_state_flag = princely_state
					has_state_flag = enemy_campaign_running
					state = 424
					state = 431
					state = 445
				}
				is_owned_by = RAJ
				is_core_of = RAJ
			}
			has_country_flag = election_enabled
		}
		target_trigger = {
			FROM = {
				is_owned_by = RAJ
				is_core_of = RAJ
			}
		}
	}
	
	inc_enemy_operation = {
		icon = generic_nationalism
		on_map_mode = map_and_decisions_view
		state_target = yes
		available = {
			always = no
		}
		activation = {
			always = no
		}
		highlight_state_targets = {
			state = var:enemy_campaign_state
		}
		is_good = no
		timeout_effect = {
			var:enemy_campaign_state = { add_to_variable = { inc = 1 } custom_effect_tooltip = raj_inc_1 }
			RAJ = { clr_country_flag = inc_operation }
			var:enemy_campaign_state = { 
				clr_state_flag = enemy_campaign_running 
				clr_state_flag = raj_enemy_op 
				clr_state_flag = enemy_campaign_running
			}
			hidden_effect = { 
				every_owned_state = {
					clr_state_flag = enemy_campaign_running
				}	
			}
			set_variable = {
				enemy_campaign_state = 0
			}
			raj_calculate_election = yes
			country_event = { 
				id = india.3
				days = 0
			}
		}
		days_mission_timeout = 15
		visible = {
			has_country_flag = election_enabled
		}
		target_trigger = {
			FROM = {
				NOT = {
					has_state_flag = princely_state
					has_state_flag = enemy_campaign_running
					state = 424
					state = 431
					state = 445
				}
				is_owned_by = RAJ
				is_core_of = RAJ
				
			}
			FROM = {
				has_state_flag = raj_enemy_op
			}
			RAJ = { has_country_flag = inc_operation }
		}
	}
	upa_enemy_operation = {
		icon = generic_nationalism
		on_map_mode = map_and_decisions_view
		state_target = yes
		available = {
			always = no
		}
		highlight_state_targets = {
			state = var:enemy_campaign_state
		}
		activation = {
			always = no
		}
		is_good = no
		timeout_effect = {
			var:enemy_campaign_state = { add_to_variable = { upa = 1 } custom_effect_tooltip = raj_upa_1 }
			RAJ = { clr_country_flag = upa_operation }
			var:enemy_campaign_state = { 
				clr_state_flag = enemy_campaign_running 
				clr_state_flag = raj_enemy_op 
				clr_state_flag = enemy_campaign_running
			}
			hidden_effect = { 
				every_owned_state = {
					clr_state_flag = enemy_campaign_running
				}	
			}
			set_variable = {
				enemy_campaign_state = 0
			}
			raj_calculate_election = yes
			country_event = { 
				id = india.3
				days = 0
			}
		}
		days_mission_timeout = 15
		visible = {
			has_country_flag = election_enabled
		}
		target_trigger = {
			FROM = {
				NOT = {
					has_state_flag = princely_state
					has_state_flag = enemy_campaign_running
					state = 424
					state = 431
					state = 445
				}
				is_owned_by = RAJ
				is_core_of = RAJ
			}
			FROM = {
				has_state_flag = raj_enemy_op
			}
			RAJ = { has_country_flag = upa_operation }
		}
	}
	ifb_enemy_operation = {
		icon = generic_nationalism
		on_map_mode = map_and_decisions_view
		state_target = yes
		highlight_state_targets = {
			state = var:enemy_campaign_state
		}
		available = {
			always = no
		}
		activation = {
			always = no
		}
		is_good = no
		timeout_effect = {
			var:enemy_campaign_state = { add_to_variable = { ifb = 1 } custom_effect_tooltip = raj_ifb_1 }
			RAJ = { clr_country_flag = ifb_operation }
			var:enemy_campaign_state = { 
				clr_state_flag = enemy_campaign_running 
				clr_state_flag = raj_enemy_op 
				clr_state_flag = enemy_campaign_running
			}
			hidden_effect = { 
				every_owned_state = {
					clr_state_flag = enemy_campaign_running
				}	
			}
			set_variable = {
				enemy_campaign_state = 0
			}
			raj_calculate_election = yes
			country_event = { 
				id = india.3
				days = 0
			}
			
		}
		days_mission_timeout = 15
		visible = {
			has_country_flag = election_enabled
		}
		target_trigger = {
			FROM = {
				NOT = {
					has_state_flag = princely_state
					has_state_flag = enemy_campaign_running
					state = 424
					state = 431
					state = 445
				}
				is_owned_by = RAJ
				is_core_of = RAJ
			}
			FROM = {
				has_state_flag = raj_enemy_op
			}
			RAJ = { has_country_flag = ifb_operation }
		}
	}
}	