BLR_greater_belarus_decision_category_r56 = {
	# Assimilate the Western Territories
	BLR_assimilate_bialystok_r56 = {
		icon = GFX_decision_generic_operation
		ai_will_do = { base = 100 }

		days_remove = 85
		fire_only_once = yes

		available = {
			custom_trigger_tooltip = {
                tooltip = LIE_to_core_donrepublic
                97 = { # Bialystok
                    is_controlled_by = ROOT
                    compliance > 35
                    resistance < 5
                }
                934 = { # Suwalki
                    is_controlled_by = ROOT
                    compliance > 35
                    resistance < 5
                }
            }
		}

		visible = {
			NOT = {
				97 = { is_core_of = ROOT } # Bialystok
				934 = { is_core_of = ROOT } # Suwalki
			}
		}

		highlight_states = {
			highlight_state_targets = {
				state = 97 # Bialystok
				state = 934 # Suwalki
			}
		}

		modifier = {
            political_power_factor = -0.1
            consumer_goods_factor = 0.08
        }

		complete_effect = {
			every_state = {
				limit = {
					NOT = { is_claimed_by = ROOT }
					OR = {
						state = 97 # Bialystok
						state = 934 # Suwalki
					}
				}
				add_claim_by = ROOT
			}
		}

		remove_effect = {
			add_political_power = 35
			add_popularity = {
				ideology = ROOT
				popularity = 0.03
			}

			custom_effect_tooltip = generic_skip_one_line_tt

			every_state = {
				limit = {
					NOT = { is_core_of = ROOT }
					OR = {
						state = 97 # Bialystok
						state = 934 # Suwalki
					}
				}
				add_core_of = ROOT
				transfer_state_to = ROOT
			}

			hidden_effect = {
				every_state = {
					limit = {
						NOT = { is_claimed_by = ROOT }
						OR = {
							state = 97 # Bialystok
							state = 934 # Suwalki
						}
					}
					remove_claim_by = ROOT
				}
			}
		}
	}

	# Reintegrate the Eastern Territories
	BLR_assimilate_smolensk_r56 = {
		icon = GFX_decision_generic_protection
		ai_will_do = { base = 100 }

		days_remove = 85
		fire_only_once = yes

		available = {
			custom_trigger_tooltip = {
                tooltip = LIE_to_core_donrepublic
                243 = { # Roslavl
                    is_controlled_by = ROOT
                    compliance > 35
                    resistance < 5
                }
                242 = { # Smolensk
                    is_controlled_by = ROOT
                    compliance > 35
                    resistance < 5
                }
            }
		}

		visible = {
			NOT = {
				243 = { is_core_of = ROOT } # Roslavl
				242 = { is_core_of = ROOT } # Smolensk
			}
		}

		highlight_states = {
			highlight_state_targets = {
				state = 243 # Roslavl
				state = 242 # Smolensk
			}
		}

		modifier = {
            political_power_factor = -0.15
            consumer_goods_factor = 0.12
        }

		complete_effect = {
			every_state = {
				limit = {
					NOT = { is_claimed_by = ROOT }
					OR = {
						state = 243 # Roslavl
						state = 242 # Smolensk
					}
				}
				add_claim_by = ROOT
			}
		}

		remove_effect = {
			add_political_power = 65
			add_stability = 0.05

			custom_effect_tooltip = generic_skip_one_line_tt

			every_state = {
				limit = {
					NOT = { is_core_of = ROOT }
					OR = {
						state = 243 # Roslavl
						state = 242 # Smolensk
					}
				}
				add_core_of = ROOT
				transfer_state_to = ROOT
			}

			hidden_effect = {
				every_state = {
					limit = {
						NOT = { is_claimed_by = ROOT }
						OR = {
							state = 243 # Roslavl
							state = 242 # Smolensk
						}
					}
					remove_claim_by = ROOT
				}
			}
		}
	}
}