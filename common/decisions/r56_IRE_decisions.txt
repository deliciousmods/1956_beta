r56_ire_irish_politics = {

	r56_IRE_ira_activity = {

		icon = generic_ignite_civil_war

		available = {
			original_tag = IRE
		}

		priority = 10

		days_mission_timeout = 70

		priority = 10

		fire_only_once = yes

		activation = {
			tag = IRE
		}

		visible = {
			has_civil_war = no
		}

		cancel_if_not_visible = yes

		is_good = no

		timeout_effect = {
			
		}
	}

	
	r56_IRE_1937_election = {

		icon = generic_ignite_civil_war

		available = {
			original_tag = IRE
		}

		priority = 11

		days_mission_timeout = 547

		fire_only_once = yes

		activation = {
			tag = IRE
		}

		visible = {
			has_civil_war = no
		}

		cancel_if_not_visible = yes

		is_good = no

		timeout_effect = {
			
		}
	}

	r56_IRE_ira_activity = {

		icon = generic_ignite_civil_war

		available = {
			original_tag = IRE
		}

		priority = 10

		fire_only_once = yes

		activation = {
			tag = IRE
		}

		visible = {
			has_civil_war = no
		}

		cancel_if_not_visible = yes

		is_good = no

		timeout_effect = {
		
		}
	}

	#Decisions just use a check state flag systerm. Not complex enough to warrant a varrible based solution. - SpicyAlfredo
	IRE_fine_gael_campaigning = { 
		icon = generic_political_rally
		on_map_mode = map_only
		state_target = yes

		visible = {
			FROM = {
				is_owned_by = IRE
				is_core_of = IRE
			}
		}

		target_array = controlled_states

		available = {	
			original_tag = IRE		
		}

		allowed = {
			original_tag = IRE
		}
		

		ai_will_do = {
			factor = 10
		}

		cost = 25

			
		days_remove = 5

		visible = {
			original_tag = IRE
			ROOT = {
				has_completed_focus = IRE_fine_gael
			}
			FROM = {
				NOT = {
					has_state_flag = r56_fine_gael_control
				}
			}
		}

		modifier = {
			
		}
		
		remove_effect = {	
			FROM = {
				clr_state_flag = r56_fine_fail_control
				set_state_flag = r56_fine_gael_control
			}
		}		
	}

	IRE_fine_gael_control_maker = { 
		icon = congress
		on_map_mode = map_only
		state_target = yes
		visible = {
			FROM = {
				is_owned_by = IRE
				is_core_of = IRE
				has_state_flag = r56_fine_gael_control
			}
		}

		target_array = controlled_states
		target_trigger = {
			FROM = {
				is_owned_by = IRE
				is_core_of = IRE
				has_state_flag = r56_fine_gael_control
			}
		}

		available = {	
			original_tag = IRE		
			has_country_flag = r56_always_false
		}

		allowed = {
			original_tag = IRE
		}

		
		fire_only_once = no
	

		ai_will_do = {
			factor = 10
		}

		visible = {
			original_tag = IRE
			any_owned_state = {
				has_state_flag = r56_fine_gael_control
			}
		}

		modifier = {
			
		}
		
		remove_effect = {	
			
		}		
	}

	IRE_fine_fail_control_maker = { 
		icon = generic_political_discourse
		on_map_mode = map_only
		state_target = yes
		visible = {
			FROM = {
				is_owned_by = IRE
				is_core_of = IRE
				has_state_flag = r56_fine_fail_control
			}
		}

		target_array = controlled_states
		target_trigger = {
			FROM = {
				is_owned_by = IRE
				is_core_of = IRE
				has_state_flag = r56_fine_fail_control
			}
		}

		available = {	
			original_tag = IRE		
			has_country_flag = r56_always_false
		}

		allowed = {
			original_tag = IRE
		}

		
		fire_only_once = no
	

		ai_will_do = {
			factor = 10
		}

		visible = {
			original_tag = IRE
			any_owned_state = {
				has_state_flag = r56_fine_fail_control
			}
		}

		modifier = {
			
		}
		
		remove_effect = {	
			
		}		
	}
}