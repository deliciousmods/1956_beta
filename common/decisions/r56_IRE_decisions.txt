r56_IRE_IRA_activity = {
	r56_IRE_IRA_activity_mission = {

		icon = generic_ignite_civil_war

		available = {
			hidden_trigger = { always = no }
		}

		priority = 11

		days_mission_timeout = IRE_OG_IRA_activity_variable

		activation = {
			tag = IRE
		}

		visible = {
			has_civil_war = no
		}

		cancel_if_not_visible = yes

		is_good = no

		timeout_effect = {
			custom_effect_tooltip = r56_IRE_IRA_activity_mission_tt
		}
	}

	r56_IRE_crackdown_on_IRA_recruitment = {
		icon = GFX_decision_oppression
		target_trigger = {
			FROM = {
				is_core_of = ROOT
				is_fully_controlled_by = ROOT
				NOT = {
					has_state_flag = IRE_crackdown_on_IRA_recruitment_state_flag
				}
			}
		}

		target_array = ROOT.core_states
		
		state_target = yes
		on_map_mode = map_and_decisions_view

		cost = 25
		days_remove = 35

		visible = {
			has_completed_focus = IRE_continue_ira_crackdowns
		}

		available = {
			NOT = {
				has_country_flag = r56_IRE_continue_ira_crackdowns_cd
			}
			FROM = {
				NOT = {
					has_state_flag = IRE_crackdown_on_IRA_recruitment_state_flag
				}
			}
		}

		complete_effect = {
			set_country_flag = r56_IRE_continue_ira_crackdowns_cd
			FROM = {
				set_state_flag = IRE_crackdown_on_IRA_recruitment_state_flag
				remove_core_of = IRA
			}
		}

		remove_effect = {
			clr_country_flag = r56_IRE_continue_ira_crackdowns_cd
		}

		ai_will_do = {
			base = 10

			modifier = {
				NOT = {
					is_historical_focus_on = no
					has_war_with_major = no
				}
				factor = 25
			}
		}
	}
}



r56_IRE_passive_assistance = {

	r56_IRE_return_down_pilots = { 

		available = {
			NOT = {
				has_war_with = ENG
			}
		}


		visible = {
			has_completed_focus = IRE_passive_assistance
		}

	}
}

r56_IRE_in_the_empires_shadow = {
	r56_IRE_trade_talk_missions = {

		icon = generic_ignite_civil_war

		available = {
			hidden_trigger = { always = no }
			ROOT = {
				has_civil_war = NO
			}
			AND = {
				NOT = {
					has_war_with = ENG
				}
				ENG = {
					exists = yes
					has_capitulated = yes
					has_opinion = { target = ROOT value > -101 }
				}
			}
		}

		priority = 10

		days_mission_timeout = 70

		activation = {
			AND = {
				has_country_flag = r56_IRE_trade_talks_go
				hidden_trigger = {
					ROOT = {
						OR = {
							has_idea = IRE_anglo_irish_trade_war_5
							has_idea = IRE_anglo_irish_trade_war_4
							has_idea = IRE_anglo_irish_trade_war_3
							has_idea = IRE_anglo_irish_trade_war_2
							has_idea = IRE_anglo_irish_trade_war_1
							has_idea = IRE_anglo_irish_trade_war
						}
					}
				}
			}
		}

		visible = {
			ENG = {	
				OR = {
					exists = no
					has_capitulated = yes
				}
			}
			hidden_trigger = {
				ROOT = {
					OR = {
						has_idea = IRE_anglo_irish_trade_war_5
						has_idea = IRE_anglo_irish_trade_war_4
						has_idea = IRE_anglo_irish_trade_war_3
						has_idea = IRE_anglo_irish_trade_war_2
						has_idea = IRE_anglo_irish_trade_war_1
						has_idea = IRE_anglo_irish_trade_war
					}
				}
			}
		}

		cancel_if_not_visible = yes

		is_good = yes

		complete_effect = {
			effect_tooltip = { #triggers are on event for the sake of flexiblity/anti-cheese
				country_event = { id = r56.ire.9 } 
				event_option_tooltip = r56.ire.9.a
			}
		}

		timeout_effect = {
			random_list = { #50/50
				50 = { #Talks Progress
					country_event = { id = r56.ire.7 } 
					event_option_tooltip = r56.ire.7.a
				}
				50 = { #Talks Stall
					country_event = { id = r56.ire.8 }
					event_option_tooltip = r56.ire.8.a
				}
			}
		}
	}
}

political_actions = { 

	r56_IRE_hold_snap_elections = {

		icon = generic_police_action

		priority = 100

		available = {
			ROOT = {
				exists = yes
				has_government = democratic		
			}			
		}

		allowed = {
			original_tag = IRE
		}

		cost = 25
		
		fire_only_once = yes
		
		days_remove = 30

		ai_will_do = {
			factor = 10
		}

		visible = {
			ROOT = {
				has_completed_focus = IRE_undermine_dev
			}
		}
		
		remove_effect = {	
			hold_election = ROOT			
		}		
	}
}

