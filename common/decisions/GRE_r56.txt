GRE_divided_nation_category = {

  GRE_communist_insurrection = {

		icon = generic_ignite_civil_war

		available = {
			hidden_trigger = { 			
				has_country_flag = r56_commie_crisis
				OR = {
					NOT = {
						has_country_flag = gre_bftb_focus_tree_cf
					}
					NOT = {
						has_dlc = "Battle for the Bosporus"
					}
				}
			}			
		}

		priority = 10

		days_mission_timeout = 135

		fire_only_once = yes

		activation = {
			AND = {
					NOT = { #just in case
						has_country_flag = gre_bftb_focus_tree_cf
					}
					OR = {
						NOT = {
							has_game_rule = {
								rule = GRE_focus_tree_selection
								option = STANDARD_TREE
							}
						}
						NOT = {
							has_dlc = "Battle for the Bosporus"
						}
					}
					original_tag = GRE
				}
			}

		visible = {
			AND = {
				hidden_trigger = { 
					NOT = { #just in case
						has_country_flag = gre_bftb_focus_tree_cf
					}
					OR = {
						NOT = {
							has_game_rule = {
								rule = GRE_focus_tree_selection
								option = STANDARD_TREE
							}
						}
						NOT = {
							has_dlc = "Battle for the Bosporus"
						}
					}
				}
				original_tag = GRE
			}
		}

		cancel_if_not_visible = yes

		is_good = no

		timeout_effect = {
		GRE = { country_event = { hours = 1 id = greece.81 } }
			hidden_effect = {
				set_country_flag = r56_commie_crisis
			}
   		}
  	}

  GRE_legislative_elections = {

		icon = generic_political_rally

		available = {
			hidden_trigger = { 			
				has_country_flag = GRE_legislative_elections_r56
				NOT = {
					has_country_flag = gre_bftb_focus_tree_cf
				}
				OR = {
					NOT = {
						has_game_rule = {
							rule = GRE_focus_tree_selection
							option = STANDARD_TREE
						}
					}
					NOT = {
						has_dlc = "Battle for the Bosporus"
					}
				}
			}
		}

		priority = 8

		days_mission_timeout = 22

		fire_only_once = yes

		activation = {
			AND = {
				hidden_trigger = { 
					NOT = { #just in case
						has_country_flag = gre_bftb_focus_tree_cf
					}
					OR = {
						has_game_rule = {
							rule = GRE_focus_tree_selection
							option = R56_TREE
						}
						NOT = {
							has_dlc = "Battle for the Bosporus"
						}
					}
				}
				original_tag = GRE
			}	
		}

		visible = { 
			original_tag = GRE
			hidden_trigger = { 
				OR = {
					has_game_rule = {
						rule = GRE_focus_tree_selection
						option = R56_TREE
					}
					NOT = {
						has_dlc = "Battle for the Bosporus"
					}
				}	
			}
		}

		cancel_if_not_visible = yes

		is_good = yes

		timeout_effect = {
			hidden_effect = {
				set_country_flag = GRE_legislative_elections_r56
				set_country_flag = r56_demerzis_crisis
			}
			GRE = { country_event = { hours = 8 id = greece.43 } }
    	}
  }

  GRE_demertzis_illing_health = {

		icon = decision_revolt

		available = {
			hidden_trigger = { 	
				has_country_flag = r56_demerzis_illness
			}
    	}
    
		visible = {
			original_tag = GRE
			has_country_flag = r56_demerzis_crisis
		}

		priority = 9

		days_mission_timeout = 82

		fire_only_once = yes

		activation = {
			original_tag = GRE
			has_country_flag = r56_demerzis_crisis
			NOT = {
				has_country_flag = gre_bftb_focus_tree_cf
			}
			OR = {
				NOT = {
					has_game_rule = {
						rule = GRE_focus_tree_selection
						option = STANDARD_TREE
					}
				}
				NOT = {
					has_dlc = "Battle for the Bosporus"
				}
			}	
		}

		cancel_if_not_visible = no

		is_good = no

		timeout_effect = {
			set_country_flag = r56_demerzis_illness
			GRE = { country_event = { hours = 8 id = greece.5 } }
    	}
  	}

	GRE_work_on_stability = {

		icon = generic_scales

		available = {
			NOT = {
				has_country_flag = gre_bftb_focus_tree_cf
			}	
		}

		allowed = {
			tag = GRE
		}

		ai_will_do = {
			factor = 100
		}

		fire_only_once = yes

		modifier = {
			political_power_gain = -0.1
		}

		days_remove = 120

		cost = 75

		complete_effect = {
            add_stability = 0.05
		}
	}

	GRE_work_on_economy = {

        icon = gre_paying_ifc_debt

        available = {
			has_completed_focus = GRE_end_the_depression
		}

		allowed = {
			original_tag = GRE
		}

		visible = {
			has_completed_focus = GRE_end_the_depression
			NOT = {
				has_country_flag = gre_bftb_focus_tree_cf
			}	
	    }

      ai_will_do = {
  			factor = 100
  		}

		ai_will_do = {
			factor = 100
		}

		fire_only_once = yes

		modifier = {
			industrial_capacity_factory = -0.1
		}

		days_remove = 30

		cost = 5

		complete_effect = {
            		modify_timed_idea = {
					idea = GRE_depression_recovery
					days = -30
			}
		}
	}

	GRE_work_on_government = {

        icon = decision_generic_political_discourse

        available = {
			has_completed_focus = GRE_divided_nation
		}

		allowed = {
			original_tag = GRE
		}

		visible = {
			NOT = {
				has_country_flag = gre_bftb_focus_tree_cf
			}	
	    }

     	ai_will_do = {
  			factor = 100
  		}

		fire_only_once = yes

		modifier = {
			political_power_gain = -0.1
		}

		days_remove = 50

		cost = 50

		complete_effect = {
            remove_ideas = GRE_government_crisis
		}
	}
}

GRE_honor_byzantium_category = {

    GRE_constantinople_capital = {

        icon = GFX_decision_generic_nationalism

        available = {
			has_completed_focus = GRE_honor_byzantium
			797 = { is_fully_controlled_by = ROOT }
		}

		visible = {
            has_completed_focus = GRE_honor_byzantium
	    }

	    allowed = {
			tag = GRE
		}

      ai_will_do = {
  			factor = 50
  		}

		fire_only_once = yes

		days_remove = 5

		cost = 50

		complete_effect = {
			add_stability = 0.05
 			set_capital = { state = 797 }
		  set_province_name = { id = 9833 name = "Constantinople"}

    }
  }

    GRE_repurpose_the_hagia_sophia = {

		icon = GFX_decision_generic_nationalism

		available = {
			797 = { is_fully_controlled_by = ROOT }
		}

		visible = {
			OR = {
				has_completed_focus = GRE_honor_byzantium
				has_completed_focus = GRE_reviving_the_double_headed_eagle
			}
			
			  
	    }

      ai_will_do = {
  			factor = 50
  		}

  		allowed = {
			tag = GRE
		}

		fire_only_once = yes

		days_remove = 60

		cost = 50

    modifier = {
			civilian_factory_use = 2
		}

    remove_effect = {
      add_ideas = GRE_patriarch_support
      797 = {
        add_extra_state_shared_building_slots = 1
       }
    }
  }
}

political_actions = {

	GRE_recall_napoleon_zervas = {

	icon = attache

		fire_only_once = yes

	days_remove = 10

	custom_cost_text = napoleon_zervas_decision_cost
	custom_cost_trigger = {
		command_power > 24
		has_political_power > 24
	}

	allowed = {
		tag = GRE
	}

	ai_will_do = {
		factor = 60
	}

	available = {
		command_power > 24
		has_political_power > 24
		NOT = {
			OR = {
				has_government = communism
				has_country_leader = {
					character = GRE_konstantinos_demertzis
					ruling_only = yes
				}
			}
		}
	}

	visible = {
		OR = {
			NOT = {
				has_dlc = "Battle for the Bosporus"
			}
			has_game_rule = {
				rule = GRE_focus_tree_selection
				option = R56_TREE
			}
		}
	}

	remove_effect = {
		set_country_flag = GRE_zervas_recalled
		create_corps_commander = {
		name = "Napoleon Zervas"
		portrait_path = "gfx/leaders/GRE/r56_portrait_GRE_Napoleon_Zervas.dds"
		traits = { expert_improviser skirmisher }
		skill = 2
		id = 75009
		attack_skill = 2
		defense_skill = 2
		planning_skill = 2
		logistics_skill = 2
		}
	}
	complete_effect = {
		add_command_power = -25
		add_political_power = -25
	}
}

GRE_recall_konstantinos_ventiris = {

	icon = attache

	fire_only_once = yes

	days_remove = 10

	custom_cost_text = konstantinos_ventiris_decision_cost
	custom_cost_trigger = {
		command_power > 24
		has_political_power > 24
	}

	allowed = {
		tag = GRE
	}

	visible = {
		OR = {
			NOT = {
				has_dlc = "Battle for the Bosporus"
			}
			has_game_rule = {
				rule = GRE_focus_tree_selection
				option = R56_TREE
			}
		}
	}

	ai_will_do = {
		factor = 100
	}

	available = {
		command_power > 24
		has_political_power > 24
		NOT = {
			OR = {
				has_government = communism
				has_country_leader = {
					character = GRE_konstantinos_demertzis_r56
					ruling_only = yes
				}
			}
		}
	}

	remove_effect = {
		set_country_flag = GRE_ventiris_recalled
		show_unit_leaders_tooltip = GRE_konstantinos_ventiris
	}
	complete_effect = {
		add_command_power = -25
		add_political_power = -25
	}
}

	GRE_seize_armories = {

		icon = GFX_decision_generic_prepare_civil_war

		available = {
			has_civil_war = yes
			has_government = communism
		}

		visible = {
			has_civil_war = yes
			has_government = communism
		}

		ai_will_do = {
			factor = 50
		}

		allowed = {
			tag = GRE
		}

		fire_only_once = yes

		days_remove = 5

		cost = 50

		complete_effect = {
			country_event = { id = greece.71 }
		}
	}
}

GRE_army_reform_category = {

    GRE_promote_promising_officers = {

		icon = generic_military

		allowed = {
			tag = GRE
		}

		available = {
			has_completed_focus = GRE_army_reform
			command_power > 34
			has_political_power > 34
		}

		ai_will_do = {
			factor = 100
		}

		fire_only_once = yes

		days_remove = 50

		remove_effect = {
			swap_ideas = { remove_idea = GRE_shadow_of_1921 add_idea = GRE_shadow_of_1921_2 }
		}
		complete_effect = {
			add_command_power = -35
			add_political_power = -35
		}
  	}

	GRE_start_military_exercises = {

		icon = generic_prepare_civil_war

		available = {
			has_completed_focus = GRE_army_reform
			has_idea = GRE_shadow_of_1921_2
			command_power > 24
			has_political_power > 24
		}

		allowed = {
			tag = GRE
		}

		visible = {
			has_idea = GRE_shadow_of_1921_2
		}

		ai_will_do = {
			factor = 100
		}

		fire_only_once = yes

		days_remove = 50

		remove_effect = {
			remove_ideas = GRE_shadow_of_1921_2
			army_experience = 15
		}

		complete_effect = {
			add_command_power = -25
			add_political_power = -25
		}
	}
}

political_actions = {
    GRE_prohibit_sale_of_ships = {

        icon = generic_decision

        ai_will_do = {
			base = 40
		}

        available = {
          has_war = yes
          has_full_control_of_state = 47
        }

        allowed = {
            tag = GRE
        }
        visible = {
            has_war = yes
        }
        fire_only_once = no
	      
        modifier = {
            consumer_goods_factor = 0.1
            industrial_capacity_dockyard = 0.1
            min_export = -0.05
	      }

        days_remove = 180
        days_re_enable = 365
        
        complete_effect = {
            add_offsite_building = { type = dockyard level = 1 }
        }
  
        remove_effect = {
            add_offsite_building = { type = dockyard level = -1 }
        }
    }
}