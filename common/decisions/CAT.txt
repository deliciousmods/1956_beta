political_actions = {
#	CAT_deploy_the_aragonese_fleet = {
#
#		icon = generic_army_support
#
#		allowed = {
#			original_tag = CAT
#		}
#
#		available = {
#	
#		}
#		
#		fire_only_once = yes	
#		visible = {
#			has_global_flag = XVth_century_aragon_flag
#		}
#
#		complete_effect = {	
#			load_oob = "CAT_ARAGON_1936_naval_mtg"						
#		}			
#		ai_will_do = {
#			factor = 50
#		}
#	}
	ADR_become_catalonia = {
		allowed = { original_tag = ADR }
		icon = generic_nationalism

		available = {
			is_subject = no	
			controls_state = 165
			NOT = { country_exists = CAT }
		}
		
		cost = 25
		days_remove = 30
		fire_only_once = yes	
		
		remove_effect = {
			set_country_flag = ADR_became_catalonia_flag
			set_capital = {
				state = 165 
				remember_old_capital = no
			}
			custom_effect_tooltip = generic_skip_one_line_tt
			set_cosmetic_tag = CAT
			custom_effect_tooltip = generic_skip_one_line_tt
			165 = { add_core_of = ROOT }
			custom_effect_tooltip = generic_skip_one_line_tt
			custom_effect_tooltip = CAT_unlocks_further_decisions
			#load_focus_tree = generic_focus # breaks 
		}

		modifier = { political_power_factor = -0.25 }
		ai_will_do = { factor = 50 }
	}

	CAT_assimilate_andorra = {
		icon = generic_nationalism
		allowed = {
			OR = {
				original_tag = CAT
				original_tag = SPR
			}
		}

		cost = 10
		days_remove = 10
		fire_only_once = yes	

		available = {
			if = {
				limit = { original_tag = SPR }
				has_global_flag = scw_over
			}
			has_civil_war = no
			973 = {
				is_owned_by = ROOT
				is_fully_controlled_by = ROOT
				compliance > 40 # Andorra primarily speaks spanish and catalan, should be easier to assimilate
			}
			165 = {
				is_owned_by = ROOT
				is_controlled_by = ROOT
			}
		}

		visible = {
			165 = { is_core_of = ROOT }
			NOT = { 973 = { is_core_of = ROOT } }
		}

		remove_effect = { 973 = {  add_core_of = ROOT } }
		modifier = { political_power_factor = -0.05 consumer_goods_factor = 0.05 }
		ai_will_do = { factor = 5 }	
	}

	CAT_rekindle_catalan_in_valencia = {
		icon = generic_nationalism

		cost = 100
		fire_only_once = yes
		days_remove = 50
		
		highlight_states = {
			highlight_state_targets = {
				state = 167
			}
		}

		allowed = {
			OR = {
				original_tag = CAT
				original_tag = ADR
			}
		}

		available = {
			has_civil_war = no
			167 = {
				is_owned_by = ROOT
				is_fully_controlled_by = ROOT
				compliance > 60
			}
			167 = {
				is_owned_by = ROOT
				is_controlled_by = ROOT
			}
		}

		visible = {
			if = {
				limit = { original_tag = ADR }
				has_country_flag = ADR_became_catalonia_flag
			}
			NOT = { 167 = { is_core_of = ROOT } }
		}

		remove_effect = { 167 = { add_core_of = ROOT } }
		modifier = { political_power_factor = -0.15 consumer_goods_factor = 0.1 }
		ai_will_do = { factor = 50 }		
	}

	CAT_assimilate_the_aragonese_territories = {
		icon = generic_nationalism

		cost = 85
		fire_only_once = yes
		days_remove = 35

		highlight_states = {
			highlight_state_targets = {
				state = 794
				state = 166
			}
		}		

		allowed = {
			OR = {
				original_tag = CAT
				original_tag = ADR
			}
		}

		available = {
			has_civil_war = no
			794 = {
				is_owned_by = ROOT
				is_fully_controlled_by = ROOT
				compliance > 50
			}
			166 = {
				is_owned_by = ROOT
				is_fully_controlled_by = ROOT
				compliance > 50
			}		
		}

		visible = {
			if = {
				limit = { original_tag = ADR }
				has_country_flag = ADR_became_catalonia_flag
			}
			NOT = { 794 = { is_core_of = ROOT } }
		}

		remove_effect = {
			every_state = {
				limit = { OR = { state = 794 state = 166 } }
				add_core_of = ROOT
			}		
		}
		modifier = { political_power_factor = -0.1 consumer_goods_factor = 0.05 }
		ai_will_do = { factor = 50 }	
	}

	CAT_rekindle_catalan_in_the_baleares = {
		icon = generic_nationalism

		cost = 50
		fire_only_once = yes
		days_remove = 20

		highlight_states = {
			highlight_state_targets = {
				state = 177
			}
		}

		allowed = {
			OR = {
				original_tag = CAT
				original_tag = ADR
			}
		}

		available = {
			has_civil_war = no
			177 = {
				is_owned_by = ROOT
				is_fully_controlled_by = ROOT
				compliance > 45
			}
		}

		visible = {
			if = {
				limit = { original_tag = ADR }
				has_country_flag = ADR_became_catalonia_flag
			}
			NOT = { 177 = { is_core_of = ROOT } }
		}

		remove_effect = { 177 = { add_core_of = ROOT } }
		modifier = { political_power_factor = -0.05 }
		ai_will_do = { factor = 50 }		
	}
}
