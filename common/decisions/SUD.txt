political_actions = {
	SUD_peace_with_uk = { # yoinked from siam
		allowed = { original_tag = SUD }
		targets = { tag = ENG }
		fire_only_once = no
		days_re_enable = 365
		icon = GFX_decision_hol_exchange_intelligence_data
		visible = {
			is_subject = no
			has_completed_focus = SUD_topple_the_empire
			any_other_country = {
				has_war_with = ROOT
				OR = {
					original_tag = ENG
					is_subject_of = ENG
				}
				OR = {
					owns_state = 548	# Uganda
					owns_state = 904	# Rift Valley
					owns_state = 547	# Nairobi
					owns_state = 905	# Mombasa
					owns_state = 546	# Tanganyika
					owns_state = 1055	# Zanzibar
				}
			}
		}
		available = {
			if = {	#Too much potential to disrupt WW2
				limit = { is_faction_leader = no }
				NOT = {
					is_in_faction_with = GER
					is_in_faction_with = ITA
				}
			}
			surrender_progress < 0.1
			if = { # Uganda
				limit = { 548 = { owner = { has_war_with = ROOT } } }
				548 = { is_controlled_by_ROOT_or_subject = yes }
			}
			if = { # Nairobi
				limit = { 547 = { owner = { has_war_with = ROOT } } }
				547 = { is_controlled_by_ROOT_or_subject = yes }
			}
			if = { #  Rift Valley
				limit = { 904 = { owner = { has_war_with = ROOT } } }
				904 = { is_controlled_by_ROOT_or_subject = yes }
			}
			if = { # Mombasa
				limit = { 905 = { owner = { has_war_with = ROOT } } }
				905 = { is_controlled_by_ROOT_or_subject = yes }
			}
			if = { # Tanganyika
				limit = { 546 = { owner = { has_war_with = ROOT } } }
				546 = { is_controlled_by_ROOT_or_subject = yes }
			}
			if = { # Zanzibar
				limit = { 1055 = { owner = { has_war_with = ROOT } } }
				1055 = { is_controlled_by_ROOT_or_subject = yes }
			}
		}
		ai_will_do = { factor = 1 }
		complete_effect = {
			every_other_country = {
				limit = {
					has_war_with = ROOT
					OR = {
						original_tag = ENG
						is_subject_of = ENG
					}
					OR = {
						owns_state = 548	# Uganda
						owns_state = 904	# Rift Valley
						owns_state = 547	# Nairobi
						owns_state = 905	# Mombasa
						owns_state = 546	# Tanganyika
						owns_state = 1055	# Zanzibar
					}
				}
				if = {
					limit = { is_subject = yes }
					overlord = {
						country_event = {
							id = r56_sudan.1
							hours = 12
						}
					}
				}
				else = {
					country_event = {
						id = r56_sudan.1
						hours = 12
					}
				}
			}
			hidden_effect = {	#This giant string of if_limits is because transferring seems to must happen AFTER white peace to prevent weird issues under some circumstances.
				548 = {
					if = {
						limit = {
							is_controlled_by_ROOT_or_subject = yes
							owner = { has_war_with = ROOT }
						}
						set_state_flag = transfer_state_to_SUD_please
					}
				}
				904 = {	
					if = {
						limit = {
							is_controlled_by_ROOT_or_subject = yes
							owner = { has_war_with = ROOT }
						}
						set_state_flag = transfer_state_to_SUD_please
					}
				}
				547 = {	
					if = {
						limit = {
							is_controlled_by_ROOT_or_subject = yes
							owner = { has_war_with = ROOT }
						}
						set_state_flag = transfer_state_to_SUD_please
					}
				}
				905 = {	
					if = {
						limit = {
							is_controlled_by_ROOT_or_subject = yes
							owner = { has_war_with = ROOT }
						}
						set_state_flag = transfer_state_to_SUD_please
					}
				}
				546 = {	
					if = {
						limit = {
							is_controlled_by_ROOT_or_subject = yes
							owner = { has_war_with = ROOT }
						}
						set_state_flag = transfer_state_to_SUD_please
					}
				}
				1055 = {
					if = {
						limit = {
							is_controlled_by_ROOT_or_subject = yes
							owner = { has_war_with = ROOT }
						}
						set_state_flag = transfer_state_to_SUD_please
					}
				}
			}
		}
	}
}