ROM_return_of_the_hungarian_exiles_decision_category = {
	ROM_help_exiles_cross_border = {
		icon = infiltration
	
		available = {
			NOT = {
				has_country_flag = ROM_hun_covert_actions
				has_country_flag = ROM_hun_helped_exiles_home
			}
		}

		days_remove = 45

		cost = 50

		complete_effect = {
			set_country_flag = ROM_hun_covert_actions
			set_country_flag = ROM_hun_helped_exiles_home
		}

		remove_effect = {
			clr_country_flag = ROM_hun_covert_actions
			HUN = {
				add_popularity = {
					ideology = communism
					popularity = 0.05
				}
				add_timed_idea = {
					days = 90
					idea = r56_ROM_covert_border_crossings
				}
			}
		}
	}
	ROM_hun_smuggle_rifles_and_equipment = {
		icon = infiltration
	
		available = {
			NOT = {
				has_country_flag = ROM_hun_covert_actions
			}
			has_equipment = {
				infantry_equipment > 499
			}
			has_equipment = {
				support_equipment > 149
			}			
		}
		
		days_remove = 45

		cost = 50

		complete_effect = {
			set_country_flag = ROM_hun_covert_actions
		}

		remove_effect = {
			clr_country_flag = ROM_hun_covert_actions
			add_equipment_to_stockpile = {
				type = infantry_equipment
				amount = -500
			}
			add_equipment_to_stockpile = {
				type = support_equipment
				amount = -150
			}
			HUN = {
				add_popularity = {
					ideology = communism
					popularity = 0.05
				}
				add_timed_idea = {
					days = 90
					idea = r56_ROM_arming_militias
				}				
			}			
		}
	}
	ROM_hun_fund_rallies_in_border_towns = {
		icon = infiltration
	
		available = {
			NOT = {
				has_country_flag = ROM_hun_covert_actions
			}		
		}

		cost = 50

		days_remove = 45

		complete_effect = {
			set_country_flag = ROM_hun_covert_actions
		}
		remove_effect = {
			clr_country_flag = ROM_hun_covert_actions

			HUN = {
				add_popularity = {
					ideology = communism
					popularity = 0.05
				}
				add_timed_idea = {
					days = 90
					idea = r56_ROM_funding_worker_gatherings
				}				
			}			
		}		
	}	
	ROM_hun_pressure_government = {
		icon = infiltration
	
		available = {
			NOT = {
				has_country_flag = ROM_hun_covert_actions
			}					
			HUN = {
				communism > 0.4
			}
		}

		cost = 100

		days_remove = 30

		remove_effect = {
			HUN = { country_event = { id = rt56.rom_communist_takeover.1 } }
		}
	}		
}
ROM_provoking_the_fatherland_front_decision_category = {
	ROM_plant_romanian_communists_in_the_front = {
		icon = infiltration
	
		available = {
			NOT = {
				has_country_flag = ROM_bul_covert_actions
				has_country_flag = ROM_bul_planted_romanian_communists
			}
		}

		days_remove = 45

		cost = 50

		complete_effect = {
			set_country_flag = ROM_bul_covert_actions
			set_country_flag = ROM_bul_planted_romanian_communists
		}

		remove_effect = {
			clr_country_flag = ROM_bul_covert_actions
			BUL = {
				add_popularity = {
					ideology = communism
					popularity = 0.05
				}
				add_timed_idea = {
					days = 90
					idea = r56_ROM_covert_border_crossings
				}
			}
		}
	}
	ROM_bul_smuggle_rifles_and_equipment = {
		icon = infiltration
	
		available = {
			NOT = {
				has_country_flag = ROM_bul_covert_actions
			}
			has_equipment = {
				infantry_equipment > 499
			}
			has_equipment = {
				support_equipment > 149
			}			
		}
		
		days_remove = 45

		cost = 50

		complete_effect = {
			set_country_flag = ROM_bul_covert_actions
		}

		remove_effect = {
			clr_country_flag = ROM_bul_covert_actions
			add_equipment_to_stockpile = {
				type = infantry_equipment
				amount = -500
			}
			add_equipment_to_stockpile = {
				type = support_equipment
				amount = -150
			}
			BUL = {
				add_popularity = {
					ideology = communism
					popularity = 0.05
				}
				add_timed_idea = {
					days = 90
					idea = r56_ROM_arming_militias
				}				
			}			
		}
	}
	ROM_bul_fund_rallies_in_border_towns = {
		icon = infiltration
	
		available = {
			NOT = {
				has_country_flag = ROM_bul_covert_actions
			}		
		}

		cost = 50

		days_remove = 45

		complete_effect = {
			set_country_flag = ROM_bul_covert_actions
		}
		remove_effect = {
			clr_country_flag = ROM_bul_covert_actions

			BUL = {
				add_popularity = {
					ideology = communism
					popularity = 0.05
				}
				add_timed_idea = {
					days = 90
					idea = r56_ROM_funding_worker_gatherings
				}				
			}			
		}		
	}	
	ROM_bul_pressure_government = {
		icon = infiltration
	
		available = {
			NOT = {
				has_country_flag = ROM_bul_covert_actions
			}					
			BUL = {
				communism > 0.4
			}
		}

		cost = 100

		days_remove = 30

		remove_effect = {
			BUL = { country_event = { id = rt56.rom_communist_takeover.1 } }
		}
	}	
}
ROM_reach_out_to_yugoslav_partisans_decision_category = {
	ROM_bolster_yugoslav_communist_militias = {
		icon = infiltration
	
		available = {
			NOT = {
				has_country_flag = ROM_yug_covert_actions
				has_country_flag = ROM_yug_bolstered_communists
			}
		}

		days_remove = 45

		cost = 50

		complete_effect = {
			set_country_flag = ROM_yug_covert_actions
			set_country_flag = ROM_yug_bolstered_communists
		}

		remove_effect = {
			clr_country_flag = ROM_yug_covert_actions
			YUG = {
				add_popularity = {
					ideology = communism
					popularity = 0.05
				}
				add_timed_idea = {
					days = 90
					idea = r56_ROM_covert_border_crossings
				}
			}
		}
	}
	ROM_yug_smuggle_rifles_and_equipment = {
		icon = infiltration
	
		available = {
			NOT = {
				has_country_flag = ROM_yug_covert_actions
			}
			has_equipment = {
				infantry_equipment > 499
			}
			has_equipment = {
				support_equipment > 149
			}			
		}
		
		days_remove = 45

		cost = 50

		complete_effect = {
			set_country_flag = ROM_yug_covert_actions
		}

		remove_effect = {
			clr_country_flag = ROM_yug_covert_actions
			add_equipment_to_stockpile = {
				type = infantry_equipment
				amount = -500
			}
			add_equipment_to_stockpile = {
				type = support_equipment
				amount = -150
			}
			YUG = {
				add_popularity = {
					ideology = communism
					popularity = 0.05
				}
				add_timed_idea = {
					days = 90
					idea = r56_ROM_arming_militias
				}				
			}			
		}
	}
	ROM_yug_fund_rallies_in_border_towns = {
		icon = infiltration
	
		available = {
			NOT = {
				has_country_flag = ROM_yug_covert_actions
			}		
		}

		cost = 50

		days_remove = 45

		complete_effect = {
			set_country_flag = ROM_yug_covert_actions
		}
		remove_effect = {
			clr_country_flag = ROM_yug_covert_actions

			YUG = {
				add_popularity = {
					ideology = communism
					popularity = 0.05
				}
				add_timed_idea = {
					days = 90
					idea = r56_ROM_funding_worker_gatherings
				}				
			}			
		}		
	}	
	ROM_yug_pressure_government = {
		icon = infiltration
	
		available = {
			NOT = {
				has_country_flag = ROM_yug_covert_actions
			}					
			YUG = {
				communism > 0.4
			}
		}

		cost = 100

		days_remove = 30

		remove_effect = {
			YUG = { country_event = { id = rt56.rom_communist_takeover.1 } }
		}
	}	
}
ROM_turn_the_aegean_and_adriatic_red_decision_category = {
	ROM_alb_infiltrate_border_towns = {
		icon = infiltration
		
		visible = {
			ALB = {
				exists = yes
				NOT = { has_government = communism }
				NOT = { is_in_faction_with = ROOT }
			}
		}

		available = {
			NOT = {
				has_country_flag = ROM_alb_covert_actions
				has_country_flag = ROM_alb_infiltrated_border_towns
			}
		}

		days_remove = 45

		cost = 50

		complete_effect = {
			set_country_flag = ROM_alb_covert_actions
			set_country_flag = ROM_alb_infiltrated_border_towns
		}

		remove_effect = {
			clr_country_flag = ROM_yug_covert_actions
			ALB = {
				add_popularity = {
					ideology = communism
					popularity = 0.05
				}
				add_timed_idea = {
					days = 90
					idea = r56_ROM_covert_border_crossings
				}
			}
		}
	}
	ROM_gre_plant_balkan_spies = {
		icon = infiltration

		visible = {
			GRE = {
				exists = yes
				NOT = { has_government = communism }
				NOT = { is_in_faction_with = ROOT }
			}
		}			
		
		available = {
			NOT = {
				has_country_flag = ROM_gre_covert_actions
				has_country_flag = ROM_gre_planted_balkan_spies
			}
		}
		
		days_remove = 45
		
		cost = 50
		
		complete_effect = {
			set_country_flag = ROM_gre_covert_actions
			set_country_flag = ROM_alb_infiltrated_border_towns
		}
		
		remove_effect = {
			clr_country_flag = ROM_yug_covert_actions
			GRE = {
				add_popularity = {
					ideology = communism
					popularity = 0.05
				}
				add_timed_idea = {
					days = 90
					idea = r56_ROM_covert_border_crossings
				}
			}
		}
	}
	ROM_alb_smuggle_rifles_and_equipment = {
		icon = infiltration
	
		visible = {
			ALB = {
				exists = yes
				NOT = { has_government = communism }
				NOT = { is_in_faction_with = ROOT }
			}
		}

		available = {
			NOT = {
				has_country_flag = ROM_alb_covert_actions
			}
			has_equipment = {
				infantry_equipment > 499
			}
			has_equipment = {
				support_equipment > 149
			}			
		}
		
		days_remove = 45

		cost = 50

		complete_effect = {
			set_country_flag = ROM_alb_covert_actions
		}

		remove_effect = {
			clr_country_flag = ROM_alb_covert_actions
			add_equipment_to_stockpile = {
				type = infantry_equipment
				amount = -500
			}
			add_equipment_to_stockpile = {
				type = support_equipment
				amount = -150
			}
			ALB = {
				add_popularity = {
					ideology = communism
					popularity = 0.05
				}
				add_timed_idea = {
					days = 90
					idea = r56_ROM_arming_militias
				}				
			}			
		}
	}
	ROM_gre_smuggle_rifles_and_equipment = {
		icon = infiltration

		visible = {
			GRE = {
				exists = yes
				NOT = { has_government = communism }
				NOT = { is_in_faction_with = ROOT }
			}
		}			

		available = {
			NOT = {
				has_country_flag = ROM_gre_covert_actions
			}
			has_equipment = {
				infantry_equipment > 499
			}
			has_equipment = {
				support_equipment > 149
			}			
		}
		
		days_remove = 45
		
		cost = 50
		
		complete_effect = {
			set_country_flag = ROM_gre_covert_actions
		}
		
		remove_effect = {
			clr_country_flag = ROM_gre_covert_actions
			add_equipment_to_stockpile = {
				type = infantry_equipment
				amount = -500
			}
			add_equipment_to_stockpile = {
				type = support_equipment
				amount = -150
			}
			GRE = {
				add_popularity = {
					ideology = communism
					popularity = 0.05
				}
				add_timed_idea = {
					days = 90
					idea = r56_ROM_arming_militias
				}				
			}			
		}
	}
	ROM_alb_fund_rallies_in_border_towns = {
		icon = infiltration

		
		visible = {
			ALB = {
				exists = yes
				NOT = { has_government = communism }
				NOT = { is_in_faction_with = ROOT }
			}
		}

		available = {
			NOT = {
				has_country_flag = ROM_alb_covert_actions
			}		
		}

		cost = 50

		days_remove = 45

		complete_effect = {
			set_country_flag = ROM_alb_covert_actions
		}
		remove_effect = {
			clr_country_flag = ROM_alb_covert_actions

			ALB = {
				add_popularity = {
					ideology = communism
					popularity = 0.05
				}
				add_timed_idea = {
					days = 90
					idea = r56_ROM_funding_worker_gatherings
				}				
			}			
		}		
	}	
	ROM_gre_fund_rallies_in_border_towns = {
		icon = infiltration

		visible = {
			GRE = {
				exists = yes
				NOT = { has_government = communism }
				NOT = { is_in_faction_with = ROOT }
			}
		}			

		available = {
			NOT = {
				has_country_flag = ROM_gre_covert_actions
			}		
		}
		
		cost = 50
		
		days_remove = 45
		
		complete_effect = {
			set_country_flag = ROM_gre_covert_actions
		}
		remove_effect = {
			clr_country_flag = ROM_gre_covert_actions
			
			GRE = {
				add_popularity = {
					ideology = communism
					popularity = 0.05
				}
				add_timed_idea = {
					days = 90
					idea = r56_ROM_funding_worker_gatherings
				}				
			}			
		}		
	}	
	ROM_alb_pressure_government = {
		icon = infiltration

		visible = {
			ALB = {
				exists = yes
				NOT = { has_government = communism }
				NOT = { is_in_faction_with = ROOT }
			}
		}		

		available = {
			NOT = {
				has_country_flag = ROM_alb_covert_actions
			}					
			ALB = {
				communism > 0.4
			}
		}
	
		cost = 100
	
		days_remove = 30
	
		remove_effect = {
			ALB = { country_event = { id = rt56.rom_communist_takeover.1 } }
		}
	}
	ROM_gre_pressure_government = {
		icon = infiltration

		visible = {
			GRE = {
				exists = yes
				NOT = { has_government = communism }
				NOT = { is_in_faction_with = ROOT }
			}
		}		

		available = {
			NOT = {
				has_country_flag = ROM_gre_covert_actions
			}					
			GRE = {
				communism > 0.4
			}
		}
		
		cost = 100
		
		days_remove = 30
		
		remove_effect = {
			GRE = { country_event = { id = rt56.rom_communist_takeover.1 } }
		}
	}		
}
ROM_the_foundations_for_a_red_balkans_decision_category = {
	ROM_integrate_hungary = {
		icon = infiltration
		
		available = {
			has_capitulated = no
			NOT = {
				has_country_flag = ROM_integrating_neighbour
			}
			any_owned_state = {
				is_controlled_by = ROM
				is_core_of = HUN
				compliance > 35
				resistance < 10
			}												
		}

		fire_only_once = yes

		cost = 100

		days_remove = 35

		complete_effect = {
			set_country_flag = ROM_integrating_neighbour
		}

		remove_effect = {
			clr_country_flag = ROM_integrating_neighbour
			every_state = {
				limit = {
					is_core_of = HUN
					NOT = {
						is_core_of = ROM
					}
				}
				add_core_of = ROM 
			}
		}		
	}
	ROM_integrate_bulgaria = {
		icon = infiltration
	
		available = {
			has_capitulated = no
			NOT = {
				has_country_flag = ROM_integrating_neighbour
			}
			any_owned_state = {
				is_controlled_by = ROM
				is_core_of = BUL
				compliance > 35
				resistance < 10
			}												
		}

		fire_only_once = yes

		cost = 100

		days_remove = 35

		complete_effect = {
			set_country_flag = ROM_integrating_neighbour
		}

		remove_effect = {
			clr_country_flag = ROM_integrating_neighbour
			every_state = {
				limit = {
					is_core_of = BUL
					NOT = {
						is_core_of = ROM
					}
				}
				add_core_of = ROM 
			}
		}		
	}
	ROM_integrate_yugoslavia = {
		icon = infiltration
	
		available = {
			has_capitulated = no
			NOT = {
				has_country_flag = ROM_integrating_neighbour
			}
			any_owned_state = {
				is_controlled_by = ROM
				is_core_of = YUG
				compliance > 35
				resistance < 10
			}												
		}

		fire_only_once = yes

		cost = 100

		days_remove = 35

		complete_effect = {
			set_country_flag = ROM_integrating_neighbour
		}

		remove_effect = {
			clr_country_flag = ROM_integrating_neighbour
			every_state = {
				limit = {
					is_core_of = YUG
					NOT = {
						is_core_of = ROM
					}
				}
				add_core_of = ROM 
			}
		}		
	}
	ROM_integrate_greece = {
		icon = infiltration
	
		available = {
			has_capitulated = no
			NOT = {
				has_country_flag = ROM_integrating_neighbour
			}
			any_owned_state = {
				is_controlled_by = ROM
				is_core_of = GRE
				compliance > 35
				resistance < 10
			}												
		}

		fire_only_once = yes

		cost = 100

		days_remove = 35

		complete_effect = {
			set_country_flag = ROM_integrating_neighbour
		}

		remove_effect = {
			clr_country_flag = ROM_integrating_neighbour
			every_state = {
				limit = {
					is_core_of = GRE
					NOT = {
						is_core_of = ROM
					}
				}
				add_core_of = ROM 
			}
		}		
	}
	ROM_integrate_albania = {
		icon = infiltration
	
		available = {
			has_capitulated = no
			NOT = {
				has_country_flag = ROM_integrating_neighbour
			}
			any_owned_state = {
				is_controlled_by = ROM
				is_core_of = ALB
				compliance > 35
				resistance < 10
			}												
		}

		fire_only_once = yes

		cost = 100

		days_remove = 35

		complete_effect = {
			set_country_flag = ROM_integrating_neighbour
		}

		remove_effect = {
			clr_country_flag = ROM_integrating_neighbour
			every_state = {
				limit = {
					is_core_of = ALB
					NOT = {
						is_core_of = ROM
					}
				}
				add_core_of = ROM 
			}
		}		
	}
	ROM_integrate_bosporus = {
		icon = infiltration
	
		available = {
			has_capitulated = no
			NOT = {
				has_country_flag = ROM_integrating_neighbour
			}
			341 = {
				compliance > 35
				resistance < 10
			}
			797 = {
				compliance > 35
				resistance < 10
			}															
		}

		fire_only_once = yes

		cost = 100

		days_remove = 35

		complete_effect = {
			set_country_flag = ROM_integrating_neighbour
		}

		remove_effect = {
			clr_country_flag = ROM_integrating_neighbour
			797 = {
				add_core_of = ROM 
			}
			341 = {
				add_core_of = ROM
			}
		}		
	}				
}
ROM_subvert_moscow_authority_category = {
	ROM_prepare_defensive_battleplans = {

		visible = {
			has_completed_focus = ROM_subvert_moscow_authority
			has_war = no
		}

		fire_only_once = yes

		cost = 50

		days_remove = 90

		remove_effect = {
			custom_effect_tooltip = ROM_prepare_defensive_battleplans_tt
			set_country_flag = ROM_defensive_battleplan_prepared
		}
	}
	ROM_enact_battleplan_bessarabia = {

		visible = {
			has_completed_focus = ROM_subvert_moscow_authority
			has_war = no
		}

		available = {
			has_country_flag = ROM_defensive_battleplan_prepared
		}

		fire_only_once = yes

		complete_effect = {
			add_timed_idea = {
				idea = r56_ROM_battleplan_bessarabia_idea
				days = 90
			}
		}
	}	
	ROM_prepare_cells = {

		visible = {
			NOT = {
				has_completed_focus = ROM_establish_the_upla
			}
			is_subject_of = SOV
			has_completed_focus = ROM_prepare_anti_soviet_cells
			has_war = no
			has_equipment = {
					infantry_equipment > 1000
					artillery_equipment > 25
					support_equipment > 50
				}
			}

        state_target = yes
        target_trigger = {
            FROM = {
				is_core_of = ROOT
                is_owned_and_controlled_by = ROOT
				state_population_k > 200
            }
        }
        fire_only_once = yes

		custom_cost_text = r56_15_command_power_cost
		custom_cost_trigger = {
			command_power > 15
		}		

		complete_effect = {
			add_command_power = -15
			add_equipment_to_stockpile = {
				type = infantry_equipment
				amount = -910
				producer = ROM
			}
			add_equipment_to_stockpile = {
				type = artillery_equipment
				amount = -12
				producer = ROM
			}	
			add_equipment_to_stockpile = {
				type = support_equipment
				amount = -30
				producer = ROM
			}					
		}

        days_remove = 14
        on_map_mode = map_and_decisions_view

		remove_effect = {
			FROM = { 
				set_state_flag = prepared_anti_soviet_cell
			}
		}
		ai_will_do = {
			factor = 10
		}		
	}
	ROM_dobrudja_for_support = {

		visible = {
			has_completed_focus = ROM_reestablish_foreign_connections
			has_war = no
		}

		fire_only_once = yes

		cost = 50

		days_remove = 30

		remove_effect = {
			custom_effect_tooltip = ROM_dobruja_for_support_in_war_tt
			BUL = {
				country_event = {
					id = rt56.rom_com_bul_dobruja_trade.1 days = 2
				}
			}
		}
	}
	ROM_prepare_cells_in_ukraine = {

		visible = {
			NOT = {
				has_completed_focus = ROM_establish_the_upla
			}
			is_subject_of = SOV
			has_completed_focus = ROM_reestablish_foreign_connections
			has_war = no
			has_equipment = {
				infantry_equipment > 350
				artillery_equipment > 5
				support_equipment > 5
				}
			}

        state_target = yes
        target_trigger = {
            FROM = {
				is_core_of = UKR
                is_owned_and_controlled_by = SOV
				state_population_k > 200
				NOT = {
					state = 193 #Chernihiv
					state = 196 #Kherson
					state = 200 #Zaporizhzhia
					state = 221 #Kharkiv
					state = 225 #Sumy
					state = 227 #Donetsk
					state = 228 #Luhansk
					state = 259 #Poltava
				}
            }
        }
        fire_only_once = yes

		custom_cost_text = r56_15_command_power_cost
		custom_cost_trigger = {
			command_power > 15
		}		

		complete_effect = {
			add_command_power = -15
			add_equipment_to_stockpile = {
				type = infantry_equipment
				amount = -300
				producer = ROM
			}
			add_equipment_to_stockpile = {
				type = artillery_equipment
				amount = -1
				producer = ROM
			}	
			add_equipment_to_stockpile = {
				type = support_equipment
				amount = -1
				producer = ROM
			}					
		}

        days_remove = 14
        on_map_mode = map_and_decisions_view

		remove_effect = {
			FROM = { 
				set_state_flag = prepared_ukrainian_anti_soviet_cell
			}
		}
		ai_will_do = {
			factor = 10
		}		
	}			
}
ROM_the_electoral_deadlock_category = {
	ROM_coalition_crackdown = {

		allowed = {
			tag = ROM
		}

		available = {
			hidden_trigger = { always = no }
		}

		cancel_trigger = {
			has_completed_focus = ROM_the_romanian_civil_war
		}

		activation = {
			has_completed_focus = ROM_contacts_in_moscow
		}

		fire_only_once = yes

		days_mission_timeout = 10

		timeout_effect = {
			ROM = {
				start_civil_war = {
					ruling_party = communism
					ideology = democratic
					size = 0.7
					army_ratio = 0.7
					navy_ratio = 1
					air_ratio = 0.6
					capital = 46
					states = {
						46 955 77 76 84 83 82 81
					}
				}
			}
			hidden_effect = {
				every_other_country = {
					limit = {
						original_tag = ROM
						has_war_with = ROM
					}
					uncomplete_national_focus = {
						focus = ROM_contacts_in_moscow
						uncomplete_children = yes
						refund_political_power = no
					}
					unlock_national_focus = ROM_form_a_coalition
					unlock_national_focus = ROM_seek_western_support
					unlock_national_focus = ROM_request_balkan_aid
					unlock_national_focus = ROM_secure_the_home_front
					unlock_national_focus = ROM_the_romanian_civil_war
					complete_national_focus = ROM_concessions_to_the_right
					complete_national_focus = ROM_recruit_the_iron_guard
					add_dynamic_modifier = { modifier = ROM_romanian_naval_forces_modifier }
					add_dynamic_modifier = { modifier = ROM_romanian_air_force_modifier }
					add_dynamic_modifier = { modifier = ROM_romanian_armed_forces_modifier }
				}
			}
		}
	}
	ROM_communist_revolution = {

		icon = GFX_decision_category_USA_aid_britain

		allowed = {
			tag = ROM
		}

		available = {
			hidden_trigger = { always = no }
		}

		cancel_trigger = {
			has_completed_focus = ROM_the_romanian_civil_war
		}

		activation = {
			has_completed_focus = ROM_form_a_coalition
		}

		is_good = no

		fire_only_once = yes

		days_mission_timeout = 10

		timeout_effect = {
			ROM = {
				start_civil_war = {
					ruling_party = democratic
					ideology = communism
					size = 0.7
					army_ratio = 0.7
					navy_ratio = 1
					air_ratio = 0.6
					capital = 79
					states = {
						80 79 78 77 766 955 
					}
				}
			}
			hidden_effect = {
				every_other_country = {
					limit = {
						original_tag = ROM
						has_war_with = ROM
					}
					uncomplete_national_focus = {
						focus = ROM_form_a_coalition
						uncomplete_children = yes
						refund_political_power = no
					}
					unlock_national_focus = ROM_contacts_in_moscow
					unlock_national_focus = ROM_farmer_militias
					unlock_national_focus = ROM_ensure_eastern_loyalties
					unlock_national_focus = ROM_contact_the_unions
					unlock_national_focus = ROM_soviet_military_expertise
					unlock_national_focus = ROM_the_romanian_communist_party
					unlock_national_focus = ROM_the_romanian_civil_war
					add_dynamic_modifier = { modifier = ROM_romanian_naval_forces_modifier }
					add_dynamic_modifier = { modifier = ROM_romanian_air_force_modifier }
					add_dynamic_modifier = { modifier = ROM_romanian_armed_forces_modifier }
				}
			}
		}
	}
	ROM_ensure_eastern_loyalties_decisions = {

		icon = generic_political_discourse

		visible = {
			has_completed_focus = ROM_ensure_eastern_loyalties
		}

		available = {
			NOT = {
				has_country_flag = ROM_ensuring_loyalties
			}
		}

        state_target = yes
		targets = {
			80 79 78 766
		}
        fire_only_once = yes

        days_remove = 15
        on_map_mode = map_and_decisions_view

		complete_effect = {
			set_country_flag = ROM_ensuring_loyalties
		}

		remove_effect = {
			FROM = { 
				set_state_flag = ensured_eastern_loyalties
			}
			clr_country_flag = ROM_ensuring_loyalties
			custom_effect_tooltip = ROM_ensure_eastern_loyalties_tt
		}
		ai_will_do = {
			factor = 10
		}				
	}
	ROM_recruit_peasant_militias_decisions = {
		icon = GFX_decision_generic_shovels

		visible = {
			has_completed_focus = ROM_ensure_eastern_loyalties
		}

		available = {
			NOT = {
				has_country_flag = ROM_recruit_peasants
			}
		}

        state_target = yes
		targets = {
			80 79 78 766
		}
        fire_only_once = yes

        days_remove = 10
        on_map_mode = map_and_decisions_view

		complete_effect = {
			set_country_flag = ROM_recruit_peasants
		}

		remove_effect = {
			FROM = { 
				set_state_flag = recruited_farmers
			}
			clr_country_flag = ROM_recruit_peasants
			custom_effect_tooltip = ROM_recruiting_militias_tt
		}
		ai_will_do = {
			factor = 10
		}
	}
	ROM_seek_western_support_decisions = {
		#icon = GFX_decision_concessions

		visible = {
			#has_completed_focus = ROM_seek_western_support
		}

		available = {
			FROM = {
				NOT = { has_government = communism }
				has_opinion = {
					target = ROOT
					value > 34
				}
			}
		}
		
		target_trigger = {
			FROM = {
				OR = {
					original_tag = FRA
					original_tag = ENG
					original_tag = ITA
					original_tag = SWE
					original_tag = POL
					original_tag = USA
				}
				is_subject = no
				has_capitulated = no
				NOT = {
					has_war_with = ROOT
				}
			}
		}

		fire_only_once = yes
		cost = 25
		
		days_remove = 15

		remove_effect = {
			FROM = { country_event = { id = rt56.rom_general_election.3 } }
		}
	}
	ROM_request_balkan_aid_decisions = {
		#icon = GFX_decision_concessions

		visible = {
			#has_completed_focus = ROM_request_balkan_aid
		}

		available = {
			FROM = {
				NOT = { has_government = communism }
				has_opinion = {
					target = ROOT
					value > 34
				}
			}
		}
		
		target_trigger = {
			FROM = {
				OR = {
					original_tag = YUG
					original_tag = GRE
					original_tag = TUR
				}
				is_subject = no
				has_capitulated = no
				NOT = {
					has_war_with = ROOT
				}
			}
		}

		fire_only_once = yes
		cost = 25
		
		days_remove = 15

		remove_effect = {
			FROM = { country_event = { id = rt56.rom_general_election.2 } }
		}
	}
}