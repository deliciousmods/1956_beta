VIN_independence_war = {
	
	

	secretly_execute_ho_chi_minh = {

			icon = generic_political_discourse

			allowed = {
				original_tag = VIN
			}

			available = {
				is_subject = no
				original_tag = VIN
				custom_trigger_tooltip = {
					tooltip = VIN_only_one_action_independence_war_tt
					NOT = {
						has_country_flag = VIN_already_doing_political_action_independence_war
					}
				}
			}

			visible = {
				original_tag = VIN
			}

			complete_effect = {
			}

			ai_will_do = {
		}
	}

	secretly_execute_ta_thu_thau = {

			icon = generic_political_discourse

			allowed = {
				original_tag = VIN
			}

			available = {
				is_subject = no
				original_tag = VIN
				custom_trigger_tooltip = {
					tooltip = VIN_only_one_action_independence_war_tt
					NOT = {
						has_country_flag = VIN_already_doing_political_action_independence_war
					}
				}
			}

			visible = {
				original_tag = VIN
			}

			complete_effect = {
			}

			ai_will_do = {
		}
	}

	secretly_execute_nguyen_tuong_tam = {

			icon = generic_political_discourse

			allowed = {
				original_tag = VIN
			}

			available = {
				is_subject = no
				original_tag = VIN
				custom_trigger_tooltip = {
					tooltip = VIN_only_one_action_independence_war_tt
					NOT = {
						has_country_flag = VIN_already_doing_political_action_independence_war
					}
				}
			}

			visible = {
				original_tag = VIN
			}

			complete_effect = {
			}

			ai_will_do = {
		}
	}

	secretly_execute_cuong_de = {

			icon = generic_political_discourse

			allowed = {
				original_tag = VIN
			}

			available = {
				is_subject = no
				original_tag = VIN
				custom_trigger_tooltip = {
					tooltip = VIN_only_one_action_independence_war_tt
					NOT = {
						has_country_flag = VIN_already_doing_political_action_independence_war
					}
				}
			}

			visible = {
				original_tag = VIN
			}

			complete_effect = {
			}

			ai_will_do = {
		}
	}

	distribute_foundations_of_leninism = {

			icon = generic_book

			allowed = {
				original_tag = VIN
			}

			available = {
				is_subject = no
				original_tag = VIN
				custom_trigger_tooltip = {
					tooltip = VIN_only_one_action_independence_war_tt
					NOT = {
						has_country_flag = VIN_already_doing_political_action_independence_war
					}
				}
			}

			visible = {
				original_tag = VIN
			}

			complete_effect = {
			add_war_support = 0.05
			VIN_increase_viet_minh_influence = yes
			custom_effect_tooltip = la_lutte_lose_influence_tt
			}

			ai_will_do = {
		}
	}

	distribute_lessons_of_october = {

			icon = generic_book

			allowed = {
				original_tag = VIN
			}

			available = {
				is_subject = no
				original_tag = VIN
				custom_trigger_tooltip = {
					tooltip = VIN_only_one_action_independence_war_tt
					NOT = {
						has_country_flag = VIN_already_doing_political_action_independence_war
					}
				}
			}

			visible = {
				original_tag = VIN
			}

			cost = 25

			complete_effect = {
			add_stability = 0.05
			VIN_increase_la_lutte_influence = yes
			custom_effect_tooltip = viet_minh_lose_influence_tt
			}

			ai_will_do = {
		}
	}

	arm_caodaists_militias = {

			icon = generic_prepare_civil_war

			allowed = {
				original_tag = VIN
			}

			available = {
				is_subject = no
				original_tag = VIN
				custom_trigger_tooltip = {
					tooltip = VIN_only_one_action_independence_war_tt
					NOT = {
						has_country_flag = VIN_already_doing_political_action_independence_war
					}
				}
				controls_state = 286
			}

			visible = {
				original_tag = VIN
			}

			complete_effect = {
			custom_effect_tooltip = arm_caodaists_militias_tt
			VIN_increase_cuong_de_influence = yes
			}

			ai_will_do = {
		}
	}

	form_the_vnqdd_youth_league = {

			icon = generic_civil_support

			allowed = {
				original_tag = VIN
			}

			available = {
				is_subject = no
				original_tag = VIN
				custom_trigger_tooltip = {
					tooltip = VIN_only_one_action_independence_war_tt
					NOT = {
						has_country_flag = VIN_already_doing_political_action_independence_war
					}
				}
			}

			visible = {
				original_tag = VIN
			}

			complete_effect = {
			VIN_increase_vnqdd_influence = yes
			}

			ai_will_do = {
		}
	}

	invite_phan_boi_chau_to_the_government = {

			icon = generic_decision

			allowed = {
				original_tag = VIN
			}

			available = {
				is_subject = no
				original_tag = VIN
				custom_trigger_tooltip = {
					tooltip = VIN_only_one_action_independence_war_tt
					NOT = {
						has_country_flag = VIN_already_doing_political_action_independence_war
					}
				}
			}

			visible = {
				original_tag = VIN
			}

			complete_effect = {
			VIN_increase_vnqdd_influence = yes
			}

			ai_will_do = {
		}
	}

	assassinate_communists_officers = {

			icon = generic_anti_communism

			allowed = {
				original_tag = VIN
			}

			available = {
				is_subject = no
				original_tag = VIN
				custom_trigger_tooltip = {
					tooltip = VIN_only_one_action_independence_war_tt
					NOT = {
						has_country_flag = VIN_already_doing_political_action_independence_war
					}
				}
			}

			visible = {
				original_tag = VIN
			}

			complete_effect = {
				VIN_decrease_viet_minh_influence = yes
				VIN_decrease_la_lutte_influence = yes
			}

			ai_will_do = {
		}
	}

	black_ocean_society_support = {

			icon = jap_pacific_guardian

			allowed = {
				original_tag = VIN
			}

			available = {
				is_subject = no
				original_tag = VIN
				custom_trigger_tooltip = {
					tooltip = VIN_only_one_action_independence_war_tt
					NOT = {
						has_country_flag = VIN_already_doing_political_action_independence_war
					}
				}
			}

			visible = {
				original_tag = VIN
			}

			complete_effect = {
				VIN_increase_cuong_de_influence = yes
			}

			ai_will_do = {
		}
	}

	promote_ngo_dinh_diem_as_general = {

			icon = generic_army_support

			allowed = {
				original_tag = VIN
			}

			available = {
				is_subject = no
				original_tag = VIN
				custom_trigger_tooltip = {
					tooltip = VIN_only_one_action_independence_war_tt
					NOT = {
						has_country_flag = VIN_already_doing_political_action_independence_war
					}
				}
			}

			visible = {
				original_tag = VIN
			}

			complete_effect = {
				VIN_increase_cuong_de_influence = yes
			}

			ai_will_do = {
		}
	}

}