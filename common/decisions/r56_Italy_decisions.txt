ITA_foreign_policy_fascism = {

	ITA_renew_the_italo_yemeni_treaty = {

		icon = eng_trade_unions_support

		allowed = {
			tag = ITA
		}

		visible = {
			is_puppet = no
			has_government = fascism
		}

		target_trigger = {
               FROM = {
                    tag = YEM
               }
            }

		available = {
			YEM = {
				exists = yes
				is_puppet = no
				NOT = {
					has_war_with = ITA
					has_government = communism
					has_government = democratic
				}
			}
			custom_trigger_tooltip = {
				tooltip = "We have full control of our East African colonies and Ethiopia"
				has_full_control_of_state = 559
				has_full_control_of_state = 550
				has_full_control_of_state = 271
			}
		}

		cost = 20

		fire_only_once = yes

		visible = {
			tag = ITA
		}

		complete_effect = {
			add_opinion_modifier = {
			    target = YEM
			    modifier = ITA_renew_the_italo_yemeni_treaty_diplomacy
			}
				YEM = {
					add_opinion_modifier = {
				    target = ITA
				    modifier = ITA_renew_the_italo_yemeni_treaty_diplomacy
				}
			}
			YEM = {
				add_popularity = {
				    ideology = fascism
				    popularity = 0.05
				}
			}

			custom_effect_tooltip = ITA_renew_the_italo_yemeni_treaty_tt
		}

	}

	ITA_seize_the_balearic_islands = {

		icon = infiltrate_state

		allowed = {
			tag = ITA
		}

		visible = {
			is_puppet = no
			has_government = fascism
			SPR = {
				has_civil_war = yes
			}
		}

		target_trigger = {
               FROM = {
                    AND = {
                    	original_tag = SPR
                    	controls_state = 177
                    }
               }
            }

		available = {
			any_country = {
				original_tag = SPR
				has_civil_war = yes
				controls_state = 177
				NOT = {
					has_war_with = ITA
				}
			}
		}

		cost = 100

		fire_only_once = yes

		visible = {
			tag = ITA
		}

		complete_effect = {
			set_country_flag = seized_the_baleares_flag
			transfer_state = 177
			every_country = {
				limit = {
					original_tag = SPR
				}
				add_opinion_modifier = {
				    target = ITA
				    modifier = hostile_status
				}
				country_event = {
				    id = r56_italy.26
				    days = 1
				}
			}
			custom_effect_tooltip = ITA_seize_the_balearic_islands_tt
		}

	}

}