# categories = {
	# intel_source - the intel type used to detect this raid. air, naval, army or civilian
	# visible - when the category should be visible or not (country scope)
	# available - when the category should be available or not (country scope)
	# free_targeting = yes/no - If set, the UI will let you use this category to target any province (e.g. nuclear raids), raid types still require  province = any

	# naval_raids = { #R56
	# 	intel_source = naval
	# 	available = {
	# 	    has_navy_size = {
	# 			size > 0
	# 		}
	# 	}
	# }

# }